# Green ATM Tracker — ผลกระทบ และแผนพัฒนาต่อ

> เอกสารนี้เขียนจาก**สถานะจริงของระบบ ณ 16 ก.ย. 2569** ไม่ใช่จากที่ตั้งใจจะทำ
> ตัวเลขทุกตัวดึงจากฐานข้อมูลเดโมที่รันอยู่ · สิ่งที่ยังไม่ได้ทำเขียนไว้ว่ายังไม่ได้ทำ

---

## 1 · ปัญหาที่แก้

AEROTHAI ต้องยกระดับ 24 รายการตามฟอร์ม **วว.นบ209_2569-14** และเตรียมหลักฐานให้ผู้ประเมิน
ภายนอก (CANSO Green ATM) ปัญหาไม่ใช่ "ไม่มีข้อมูล" แต่คือ **ไม่มีใครแยกออกว่าอะไรคือ
"รายงานว่าทำแล้ว" กับอะไรคือ "พิสูจน์ได้"**

ตัวเลขจากข้อมูลตัวอย่างที่สร้างจากรูปแบบงานจริง:

| สิ่งที่พบ | จำนวน |
|---|---:|
| รายการทั้งหมด | 24 |
| **ระดับเท่าปีที่แล้วเป๊ะ — ไม่ขยับมาหนึ่งปี** | **19** |
| **มีหลักฐานชั้น A/B ที่ยืนยันแล้วอย่างน้อย 1 ชิ้น** | **5** |
| ไม่มีเจ้าของข้อมูลในระบบ — ไม่มีใครถูกเตือน | 4 |
| ยังไม่ส่งข้อมูลรอบนี้ | 4 |
| ระดับเฉลี่ยองค์กร | 1.83 |

**19 ใน 24 ข้อไม่ขยับมาหนึ่งปี และมีแค่ 5 ข้อที่พิสูจน์ได้** — ถ้าไม่มีระบบแยกสองเรื่องนี้
รายงานจะดูดีขึ้นทุกปีโดยไม่มีอะไรเปลี่ยน

---

## 2 · ผลกระทบ

### 2.1 กฎที่ระบบบังคับ ไม่ใช่ขอความร่วมมือ

| กฎ | บังคับที่ไหน | ผลที่เปลี่ยน |
|---|---|---|
| `progress` (คนกรอก) กับ `verified` (หลักฐานยืนยันแล้ว) **ห้ามรวมกัน** | คนละคอลัมน์ในฐานข้อมูล · แสดงคู่กันทุกหน้า | กรอก 100% แล้วระดับไม่ขึ้น ถ้าไม่มีหลักฐาน |
| ระดับขึ้นได้เฉพาะเมื่อมีหลักฐาน **ชั้น A/B ที่คนยืนยันแล้ว** | `setAchievedLevel()` ปฏิเสธ | ตัวเลขระดับองค์กรอ้างอิงหลักฐานได้ทุกจุด |
| **ผู้กระทำใน audit log ห้ามเป็น `ai`** | `CHECK (actor <> 'ai')` ในตาราง | ทุกการเปลี่ยนค่าจริงสาวถึงตัวคนได้ |
| agent **เสนอ** เท่านั้น คนกดยืนยัน | เขียน `proposed_tier` ไม่แตะ `confirmed_tier` | ไม่มีตัวเลขไหนขยับเพราะโมเดล |
| ไม่มีหลักฐาน **ต้องไม่หายจากรายงาน** | Suggestion ตรวจหลักฐาน**ก่อน**ตรวจเป้าเสมอ | ข้อที่ถึงเป้าแต่ไม่มีหลักฐาน ไม่ขึ้นว่า "เสร็จสมบูรณ์" |

### 2.2 เวลาที่ประหยัดได้ — ประมาณการ ยังไม่ได้วัดจริง

| งาน | เดิม (ประมาณ) | ด้วยระบบนี้ | ที่มาของตัวเลข |
|---|---|---|---|
| คัดกรองว่าเอกสารชิ้นหนึ่งใช้เป็นหลักฐานได้ไหม | 5–15 นาที/ชิ้น | **~2 วินาที** ได้ข้อเสนอ + เหตุผล แล้วคนตรวจ | จับเวลาจริงจาก DGX: 1.8–2.4 วิ/ชิ้น |
| ตามว่ากองไหนยังไม่ส่ง / เลยกำหนด | ไล่ถามเอง | โค้ดคำนวณ + ร่างข้อความรอกดส่ง | กฎ A-DUESOON / A-OVERDUE / A-MILESTONE |
| รู้ว่าข้อไหน "ดูดีแต่พิสูจน์ไม่ได้" | แทบไม่รู้จนถึงวันประเมิน | เห็นทุกหน้า ทุกวัน | คอลัมน์ Verified |

> ⚠️ ตัวเลขคอลัมน์ "เดิม" เป็น**การประมาณจากการคุยกับทีม ยังไม่ได้จับเวลาจริง**
> ห้ามนำไปอ้างเป็นผลการวัด

### 2.3 ตัวชี้วัดที่ควรใช้วัดผลจริง (ระบบเก็บให้ได้แล้ว)

ทั้งหมดอ่านได้จากฐานข้อมูลตั้งแต่วันแรก ไม่ต้องสร้างเครื่องมือเพิ่ม:

1. **จำนวนข้อที่มีหลักฐานชั้น A/B ยืนยันแล้ว** — วันนี้ 5/24 · เป้าที่มีความหมายคือให้เลขนี้โต ไม่ใช่ให้ระดับเฉลี่ยโต
2. **ช่องว่าง `progress − verified` รายข้อ** — ข้อที่ห่างมากคือข้อที่เสี่ยงที่สุดเวลาถูกประเมิน
3. **จำนวนข้อที่ไม่ขยับมาหนึ่งปี** — วันนี้ 19/24
4. **จำนวนครั้งที่เลื่อนแผน** ต่อข้อ — กฎยกระดับนับ "จำนวนครั้ง" ไม่ใช่ "ความรุนแรง"
5. **อัตราที่ทีมกลางแก้ชั้นที่ agent เสนอ** — วัดว่า agent เชื่อถือได้แค่ไหน (เก็บใน `audit_log.overrode`)
6. **จำนวนข้อที่ไม่มีเจ้าของในระบบ** — วันนี้ 4 ข้อที่ไม่มีใครถูกเตือนเลย

### 2.4 ใครได้อะไร

| บทบาท | ได้ |
|---|---|
| **เจ้าของข้อมูล (กอง)** | รู้ว่าข้อของตัวเองขาดอะไรอยู่ · แนบไฟล์แล้วรู้ผลคัดกรองใน 2 วินาที ไม่ต้องรอรอบประชุม |
| **ทีมกลาง** | คิวงานเดียวแทนการไล่อีเมล · เห็นทุกข้อที่พิสูจน์ไม่ได้ · ทุกการตัดสินมีร่องรอย |
| **ผู้บริหาร** | ภาพองค์กรที่**แยกตัวเลขที่พิสูจน์ได้ออกจากตัวเลขที่กรอก** · การคาดการณ์ที่มีสมมติฐานกำกับ |
| **ผู้ประเมินภายนอก** | ได้เอกสารที่มีการอ้างอิง ไม่ใช่คำกล่าวอ้าง *(ยังไม่ได้ทำ — ดู M5)* |

---

## 3 · พิสูจน์แล้วแค่ไหน

### ✅ พิสูจน์แล้ว มีหลักฐานรันได้

- **agent ยิงโมเดลจริง** — DGX `nemotron-3.5-lightning` และ Qwen `qwen3.8-27b` ทั้งคู่ตอบจริง
- **tool_calls จริง (TF6)** — โมเดลเรียก tool เอง ไม่ใช่ prompt ที่พูดถึง tool · DGX 6.2 วิ · Qwen 27.4 วิ
- **อ่านเนื้อเอกสารจริง (TF5)** — ป้อนร่างแผน → ตอบ **ชั้น C** อ้างคำว่า `DRAFT` ที่อยู่**ในไฟล์**
  · ป้อนรายงานลงนาม → ตอบ **ชั้น A** อ้างการลงนามและผลที่วัดได้
- **บอกเมื่อทำไม่ได้** — ป้อนรูปภาพ → บอกเองว่าอ่านเนื้อไฟล์ไม่ได้ ไม่เดาว่ารู้
- **`agent_run` มีข้อมูลจริง (AC-06)** — บันทึกทุกครั้งรวมครั้งที่ล้ม
- **ชุดทดสอบ** — `verify:app` 202/202 · `verify:seed` 35/35 (บนข้อมูล seed สะอาด)

### ❌ ยังไม่ได้พิสูจน์ — ต้องพูดเอง ไม่ให้เขาจับได้

| ข้อ | สถานะ |
|---|---|
| ~~**AC-10 · AC-15** เอกสารรอบเดือน~~ | ✅ **ทำแล้ว 16 ก.ย.** — ส่งออก HTML/MD/JSON · ทุกประโยคมีการอ้างอิง · ทะเบียนหมายเหตุบังคับให้สิ่งที่ไม่มีหลักฐานปรากฏ · ประโยคที่ไม่มีการอ้างอิงทำให้ส่งออกล้มเหลว |
| **AC-14** ลูปตีกลับพร้อมเหตุผล | ยังไม่มี `review_log` |
| **AC-19** แชตผู้ช่วยรายกอง | ยังไม่มี |
| ความแม่นของ agent | **ยังไม่ได้วัดกับชุดข้อมูลที่มีเฉลย** — ทดสอบไปไม่กี่ชิ้น |
| ใช้กับเอกสารจริงขององค์กร | **ยังไม่เคยลอง** — เอกสารจริงส่วนใหญ่เป็นไฟล์สแกน ซึ่งระบบนี้อ่านไม่ได้ |
| การประเมินโดยผู้ใช้จริง | ยังไม่มีใครนอกทีมลองใช้ |

---

## 4 · แผนพัฒนาต่อ

> ประมาณการเวลาเป็น **ชั่วโมงทำงานของคนเดียว** ไม่รวมเวลารออนุมัติหรือเวลาประชุม

### เฟส 0 — ปิดช่องที่จะทำให้ระบบเสียความน่าเชื่อถือทันที `~4 ชม.`

ทำก่อนให้คนนอกทีมแตะ

| # | งาน | ทำไมต้องก่อน | ชม. |
|:-:|---|---|:-:|
| 0.1 | **ทดสอบ clone จากศูนย์แล้ว build** บนอีกเครื่อง | `.gitignore` เพิ่งกลืนไฟล์หัวใจไป — ยังไม่มีใครพิสูจน์ว่า clone แล้วรันได้ | 1 |
| 0.2 | **ชุดข้อมูลมีเฉลยสำหรับวัด agent** 20–30 ชิ้น | ตอนนี้ยังตอบไม่ได้ว่า agent แม่นแค่ไหน — คำถามแรกที่ผู้ประเมินจะถาม | 2 |
| 0.3 | ให้ `agent_max_tool_calls` · `agent_timeout_seconds` **มีคนอ่านจริง** | ค่าตั้งค่าที่ไม่มีใครอ่าน อ่านเหมือนทำเสร็จแล้ว (พลาดซ้ำมา 4 รอบ) | 1 |

### เฟส 1 — จุดขายหลักที่ยังไม่มีของ `~10 ชม.`

| # | งาน | ปิดข้อไหน | ชม. |
|:-:|---|---|:-:|
| ~~1.1~~ | ✅ **ทำแล้ว 16 ก.ย.** เอกสารรอบเดือน + ทะเบียนหมายเหตุ · ส่งออก HTML/MD/JSON · ยังขาด**เลขหน้า**ในการอ้างอิง (อยู่ในเฟส 2.3) | AC-10 · AC-15 · R11 | ~~5~~ |
| 1.2 | **ลูปตีกลับพร้อมเหตุผล** — ตาราง `review_log` · สถานะคำตอบ · ส่วน "รายการที่ถูกตีกลับ" ในหน้างานของฉัน | AC-14 · R9 | 3 |
| 1.3 | **เก็บข้อความเกณฑ์รายข้อ** ลง schema แล้วให้ agent เทียบกับเกณฑ์จริง | ตอนนี้ agent ตัดสินจากลักษณะเอกสารอย่างเดียว | 2 |

> 1.1 คือประโยคที่ขายทั้งโครงการ — *"สิ่งที่ไม่มีหลักฐานต้องไม่หายจากรายงาน"*
> **ทำเสร็จแล้ว** · เหลือเฟส 1.2 กับ 1.3 ในเฟสนี้

### เฟส 2 — ให้ใช้กับเอกสารจริงได้ `~9 ชม.`

| # | งาน | เหตุผล | ชม. |
|:-:|---|---|:-:|
| 2.1 | **OCR สำหรับไฟล์สแกน** | เอกสารราชการจริงส่วนใหญ่เป็นสแกน — วันนี้อ่านไม่ได้เลย **นี่คือข้อจำกัดใหญ่สุดของระบบตอนนี้** | 4 |
| 2.2 | อ่านไฟล์ **Word / Excel** | แผนและตารางคำนวณจริงอยู่ในรูปแบบนี้ | 2 |
| 2.3 | **อ้างอิงระดับหน้า** — บอกได้ว่าข้อความอยู่หน้าไหน | CANSO ระบุว่าหลักฐานที่ไม่มีเลขหน้า/หัวข้อจะถูกตัดทิ้ง | 3 |

### เฟส 3 — ขยายการใช้งาน `~12 ชม.`

| # | งาน | ชม. |
|:-:|---|:-:|
| 3.1 | **ระบบยืนยันตัวตนจริง** — ตอนนี้สลับผู้ใช้ได้อิสระ (ต้นแบบ) · **ต้องทำก่อนเอาขึ้นเครือข่ายที่คนอื่นเข้าถึงได้** | 4 |
| 3.2 | แชตผู้ช่วยรายกอง (AC-19 · R3) | 3 |
| 3.3 | แปลงเป็นคะแนน CANSO 100 คะแนน + กฎ cliff | 3 |
| 3.4 | นำเข้า/ส่งออก Excel ให้ตรงกับที่ทีมใช้อยู่ | 2 |

### สิ่งที่ตั้งใจ**ไม่**ทำ

| ไม่ทำ | เหตุผล |
|---|---|
| ให้ระบบ **ตัดสินว่าองค์กรผ่านการประเมิน** | ไม่มีใครในระบบมีอำนาจนั้น · ระบบบอกได้แค่ว่าหลักฐานพร้อมให้ประเมินหรือยัง |
| ให้ agent **ยืนยันชั้นหลักฐานเอง** | ทั้งระบบตั้งอยู่บนเส้นแบ่งนี้ ถ้าข้ามไป คุณค่าหายทันที |
| **ส่งอีเมล/LINE/Teams อัตโนมัติ** | ระบบร่าง คนกดส่ง · การเตือนอัตโนมัติที่ผิดพลาดทำลายความเชื่อถือเร็วกว่าที่สร้าง |
| เรียกคอลัมน์ Suggestion ว่า **AI** | เป็นกฎในโค้ด ไม่ใช่โมเดล · เรียกผิดคือโฆษณาเกินจริง |
| อ้างว่าชั้น **A–D เป็นเกณฑ์ของ CANSO** | เป็นเครื่องมือคัดกรองของทีมเราเอง · คนที่รู้เอกสาร CANSO จะจับได้ |

---

## 5 · ความเสี่ยง

| ความเสี่ยง | ผล | ลดอย่างไร |
|---|---|---|
| **เอกสารจริงเป็นไฟล์สแกน** | agent อ่านไม่ได้เกือบทั้งหมด คุณค่าหลักหายไป | เฟส 2.1 · วันนี้ระบบ**บอกตรง ๆ ว่าอ่านไม่ได้** ไม่แกล้งทำเป็นรู้ |
| **agent เสนอชั้นผิดแล้วคนเชื่อตาม** | ตัวเลขที่ "พิสูจน์แล้ว" เชื่อถือไม่ได้ | ทุกข้อเสนอต้องมีคนกดยืนยัน · เก็บอัตราการแก้ไว้เป็นตัวชี้วัด · เฟส 0.2 วัดความแม่น |
| **โมเดลอ้างกฎที่ไม่ได้รับมา** | เคยเกิดแล้ว — มันอ้าง "เกิน 12 เดือน" ทั้งที่เราไม่เคยบอก | เหตุผลต้องขึ้นจอให้คนอ่านทุกครั้ง ห้ามซ่อน |
| **ไม่มีระบบยืนยันตัวตน** | ใครเข้าถึงเครือข่ายก็สวมเป็นใครก็ได้ | ห้ามเปิดออกอินเทอร์เน็ต · เฟส 3.1 ก่อนใช้จริง |
| **`agent_run` โตไม่มีขอบเขต** | ฐานข้อมูลบวมเมื่อใช้นาน | ใส่การตัดข้อมูลเก่าในเฟส 0.3 |
| **คนใช้ไปกรอก `progress` ให้สวยแทนที่จะหาหลักฐาน** | กลับไปที่ปัญหาเดิม | ตัวชี้วัดขององค์กรต้องเป็น **จำนวนข้อที่มีหลักฐาน A/B** ไม่ใช่ระดับเฉลี่ย |

---

## 6 · อะไรที่จะทำให้โครงการนี้ล้มเหลว

เรียงจากที่น่ากลัวที่สุด:

1. **ถ้า `progress` กับ `verified` ถูกรวมเป็นตัวเดียว** ไม่ว่าด้วยเหตุผลใด — คุณค่าทั้งหมดหายในบรรทัดเดียว
2. **ถ้าให้ agent ยืนยันเอง** เพราะ "คนกดไม่ทัน" — ระบบกลายเป็นเครื่องผลิตตัวเลขที่ไม่มีใครรับผิดชอบ
3. **ถ้าเอกสารรอบเดือน (เฟส 1.1) ไม่ได้ทำ** — ระบบสวยบนจอแต่เวลาประเมินจริงยังต้องทำมือเหมือนเดิม
4. **ถ้าไม่มีใครวัดความแม่นของ agent** — วันหนึ่งจะมีคนถาม แล้วตอบไม่ได้

---

## 7 · สิ่งที่ต้องตัดสินใจโดยคน ไม่ใช่โดยทีมพัฒนา

1. จะยอมให้ **OCR** อ่านเอกสารภายในหรือไม่ และเอกสารชั้นความลับระดับไหนที่ห้ามส่งเข้าโมเดล
2. จะใช้ **โมเดลบนเครือข่ายภายใน (DGX)** หรือ **บนคลาวด์ (Qwen FPT)** — วันนี้ระบบสลับได้ แต่ข้อกำหนดเรื่องข้อมูลยังไม่มีใครตัดสิน
3. ใครเป็นเจ้าของ **4 รายการที่วันนี้ไม่มีเจ้าของในระบบ** — ไม่มีใครถูกเตือนเลย
4. ตัวชี้วัดขององค์กรจะใช้ **"ระดับเฉลี่ย"** หรือ **"จำนวนข้อที่พิสูจน์ได้"** — เลือกอย่างแรกจะได้พฤติกรรมแบบเดิม

---

*เอกสารนี้อ้างอิงข้อมูลสังเคราะห์ทั้งหมด · ผู้รับผิดชอบระบุเป็นชื่อตำแหน่ง ไม่ใช่ชื่อบุคคลจริง*
