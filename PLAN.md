# PLAN.md — ระบบหลักฐานและความพร้อม GreenATM

**GreenATM Evidence & Readiness Platform** · AEROTHAI Agentic AI Bootcamp

> ป้ายกำกับที่ใช้ทั้งฉบับ: **`[ยืนยันแล้ว]`** = ผู้เข้าร่วมระบุเอง · **`[เสนอ]`** = ค่าตั้งต้นที่เสนอ รอยืนยัน ·
> **`[คำถามค้าง]`** = ยังไม่มีคำตอบ · **`[ไม่ทดสอบ]`** = ยังไม่มีหลักฐานจริง
> 🇬🇧 English version: `PLAN.en.md`

---

## สรุปสำหรับ Mentor Review

| Area | เนื้อหา |
|---|---|
| **Problem statement** | สำหรับ **ทีมกลางที่รับผิดชอบงาน GreenATM (~5 คน) และเจ้าของข้อมูลในแต่ละกอง**, การ **ตอบให้ได้ว่า "ตอนนี้พร้อมแค่ไหน หลักฐานพอหรือยัง และต้องเขียนคำตอบว่าอย่างไร"** เป็นเรื่องยาก เพราะ **หลักฐานกระจายอยู่หลายกองคนละรูปแบบ ส่งกันทางอีเมล ต้องตีความเองว่าเอกสารชิ้นไหนตรงเกณฑ์ข้อไหน และ "งานรายงานว่าเสร็จ" ไม่เท่ากับ "มีหลักฐานที่ผู้ตรวจรับได้"** — กว่าจะรู้ว่าขาดอะไรก็ปลายรอบแล้ว |
| **Workflow + AI role** | **กองคุยกับ agent ผู้ช่วยในแชต** — พิมพ์อัปเดตหรือลากไฟล์หลักฐานเข้าไป → **agent ตีความแล้วขึ้นการ์ดยืนยัน คนกดเอง** (agent ไม่เขียนค่าลงฐาน) → **โค้ด** คำนวณถ่วงน้ำหนักและตัดสินสถานะทั้งหมด → **agent ตัวที่สอง** ไล่อ่านหลักฐานของรายการหนึ่ง เทียบกับเกณฑ์ ตัดสินว่าพอหรือยัง **จัดชั้น A–D และระบุว่ายังขาดอะไร ตอบกลับในแชตทันที** → **โมเดล** ร่างคำตอบจากหลักฐานที่ยืนยันแล้ว รวมถึง suggestion สำหรับแก้ไขปัญหา → **คนตรวจ ยืนยัน หรือตีกลับพร้อมเหตุผล** → ผู้บริหารอนุมัติ → **dashboard + เอกสารรายเดือน Word/PDF ที่ทุกข้อความมีตัวอ้างอิง** · ผู้ตรวจประเมินอยู่นอกระบบ ระบบเตรียมหลักฐานให้ ไม่ตัดสินแทน |
| **Data** | **available**: ชุดข้อมูลตัวอย่างที่สร้างเอง — 12 รายการ (ข้อประเมิน + โครงการ) · เอกสารหลักฐาน ~24 ชิ้น · แบบฟอร์มคำตอบต้นแบบ **missing**: ไฟล์ความคืบหน้าจริงจากกอง · ข้อมูลเที่ยวบิน/emission factor **restricted**: SharePoint · AD/SSO · SMTP องค์กร · เกณฑ์ประเมินฉบับจริง **available · ทดสอบแล้ว 15 ก.ย. 2569**: endpoint โมเดล (DGX 10.0.63.215 · ไม่ต้อง auth) |
| **Prototype scope** | **ทำ:** หนึ่งเส้นทางครบวงจร — **แชตรับอัปเดต/หลักฐาน (การ์ดยืนยัน)** → คำนวณ → **agent ประเมินหลักฐาน** → คนยืนยัน → ร่างคำตอบ → อนุมัติ → dashboard + export Word/PDF รายเดือน · 3 บทบาทบนเครื่องเดียว บังคับสิทธิ์ใน backend · **ฟอร์มกรอกมือเก็บไว้เป็นทางสำรองเมื่อโมเดลล่ม** **ไม่ทำ:** นำเข้า Excel อัตโนมัติ · ส่งอีเมล/LINE จริง · SSO จริง · เชื่อมระบบเดิม · คำนวณ CO₂ · ตัวชี้วัด kWh/จำนวนรถ · OCR · deploy cloud · Alerting  deadline, archive milestone,delay  |
| **Success test** | **ปกติ:** แนบ "รายงานผลวัดที่ลงนามแล้ว" ให้รายการ A-03 → agent อ่านแล้วจัดเป็นชั้น **A** พร้อมเหตุผล → คนกดยืนยัน → ตัวเลขความพร้อมขยับ → ร่างคำตอบอ้างอิงเอกสารนั้น → อนุมัติ → เอกสารรายเดือนมีตัวอ้างอิง `[E-014]` **ล้มเหลว:** รายการ A-02 รายงานว่างานเสร็จ 100% แต่หลักฐานมีเพียง "(ร่าง) แผนดำเนินการ" → agent จัดเป็นชั้น **C** → **ความพร้อมต้องไม่ขึ้น** → ตอบว่า *"แผนไม่ใช่หลักฐานผลลัพธ์"* + **ระบุว่าต้องการหลักฐานแบบไหนเพิ่ม** → และ A-02 ต้องปรากฏใน **ทะเบียนหมายเหตุ** ท้ายเอกสาร ห้ามหายไปเงียบ ๆ |
| **Screens + design** | 5 หน้าจอ · laptop เป็นหลัก (หน้าผู้บริหารอ่านบนมือถือได้) · UI ภาษาไทย ศัพท์เทคนิคคงภาษาอังกฤษ · เอกสารส่งออกภาษาอังกฤษตามฟอร์มต้นแบบ · **หน้าแรกขึ้น "สิ่งที่ต้องตัดสิน" ก่อนตัวเลขสวย ๆ** · สถานะใช้สัญลักษณ์ควบคู่สี contrast ≥ 4.5:1 · ไม่มี hero banner ไม่มีตัวเลขที่ไม่มีที่มา `[เสนอ]` |

---

## 1. Project and review status

| | |
|---|---|
| โครงการ | **ระบบหลักฐานและความพร้อม GreenATM** (GreenATM Evidence & Readiness Platform) |
| องค์กร | AEROTHAI — Agentic AI Bootcamp 14–16 ก.ย. 2569 |
| Revision | **2.2** (จาก 2.1 — หน้าเจ้าของข้อมูลเปลี่ยนเป็นแชต §5.4.1 · ตัด M6 ไป Optional) |
| วันที่ | 15 กันยายน 2569 |
| สถานะ | **`Ready for mentor review`** — B1 ปลดแล้ว (§8) · เหลือ B2 เรื่องขอบเขต |
| กระบวนการ | `PLANNING.md` Stage 5 |
| เวลาที่เหลือ | บ่าย–เย็น 15 ก.ย. + เช้า 16 ก.ย. · **feature freeze เที่ยง 16 ก.ย.** |

---

## 2. Problem and outcome

### 2.1 ผู้ใช้และ trigger

| ผู้ใช้ | ทำอะไรวันนี้ | ปัญหาที่เจอ |
|---|---|---|
| **เจ้าของข้อมูลรายกอง** (~10 คน) | เก็บข้อมูลของกองตัวเองในรูปแบบของตัวเอง ส่งเมื่อถูกทวง | ไม่รู้ว่าต้องส่งอะไรเมื่อไร · ทำรายงานซ้ำซ้อน |
| **ทีมกลาง** (~5 คน) | ไล่รวบรวมจากทุกกอง ตีความว่าหลักฐานชิ้นไหนตรงเกณฑ์ข้อไหน แล้วเรียบเรียงคำตอบด้วยมือ | ไม่รู้ว่ากองไหนยังไม่ส่ง · ไม่รู้ว่าหลักฐานพอหรือยังจนกว่าจะอ่านครบทุกชิ้น |
| **ผู้บริหาร** (3–5 คน) | ถามเจ้าของงานเป็นราย ๆ | ไม่เห็นภาพรวม ไม่รู้ว่าอะไรเสี่ยง |
| **ผู้ตรวจประเมิน** | ตรวจเอกสารที่ทีมกลางส่งให้ | **อยู่นอกระบบโดยตั้งใจ** — ระบบเตรียมหลักฐานให้ ไม่ตัดสินแทน |

**Trigger:** รอบการประเมินประจำปี · รอบรายงานรายเดือน · เมื่อผู้บริหารขอทราบสถานะ

### 2.2 ทำไมถึงยาก

1. หลักฐานกระจายหลายกอง คนละรูปแบบ ส่งกันทางอีเมล
2. ต้องอ่านและตีความเองว่าเอกสารชิ้นไหนตรงกับเกณฑ์ข้อไหน และพอหรือยัง
3. **"งานรายงานว่าเสร็จ" ≠ "มีหลักฐานที่ผู้ตรวจรับได้"** — ไม่มีใครเห็นความต่างนี้จนถูกถาม
4. ไม่รู้ว่ากองไหนยังไม่ส่ง ต้องไล่ทวงทีละกอง
5. ตัวเลขบอกว่า "ช้า" แต่ไม่บอกว่าติดตรงไหน

### 2.3 Baseline

| | |
|---|---|
| เวลาที่ใช้รวบรวม + สรุปคำตอบหนึ่งรอบ | **ทีม 5 คน ทำงานเต็มเวลา ~2 เดือน/รอบปี** `[ยืนยันแล้ว]` |
| อัตราความผิดพลาด/ตกหล่น | **ยังไม่ทราบ** — ยังไม่เคยวัด |
| เวลาที่ใช้ติดตามความคืบหน้ารายเดือน | **ยังไม่ทราบ** |

> **ห้ามอ้างตัวเลขประหยัดเวลาหรือความแม่นยำใด ๆ** จนกว่าจะวัดหลังใช้จริง

### 2.4 ผลลัพธ์ที่ตั้งใจให้เกิด (สังเกตได้)

1. เปิดหน้าเดียวตอบได้ว่า **อะไรพร้อม / ไม่พร้อม / ติดตรงไหน** โดยไม่ต้องถามใคร
2. **ทุกข้อความในเอกสารรายเดือน คลิกกลับไปถึงหลักฐานต้นทางได้**
3. รายการที่ "เสร็จแล้วแต่ไม่มีหลักฐาน" **ถูกมองเห็นภายในรอบเดียว** ไม่ใช่ตอนผู้ตรวจมา
4. เห็นทันทีว่า **กองไหนยังไม่ส่งข้อมูล**

---

## 3. Scope

### 3.1 Required — ต้องเสร็จและเดโมได้ `[ยืนยันแล้ว]`

| # | ความสามารถ | ของจริง / จำลอง |
|:---:|---|---|
| R1 | ฐานข้อมูล + seed 12 รายการ — **ข้อคำถามประเมิน และ โครงการที่มี milestone** + เอกสาร ~24 ชิ้น (ผสมชั้น A/B/C/D ตั้งใจ) | **จริง** (ข้อมูล synthetic ติดป้ายในแอป) |
| R2 | 3 บทบาท + ปุ่มสลับ · **บังคับสิทธิ์ใน backend ไม่ใช่ซ่อนปุ่ม** | **จริง** (ผู้ใช้เป็น mock) |
| R3 ⭐ | **แชตรับอัปเดตจากกอง** — เจ้าของข้อมูลพิมพ์/แนบไฟล์ → agent ตีความ → **การ์ดยืนยัน คนกดเอง** (§5.4.1) · ฟอร์มกรอกมือยังอยู่เป็นทางสำรองเมื่อโมเดลล่ม | **จริง** |
| R4 | **อัปโหลดเอกสารหลักฐาน** (PDF/Word/รูป — ไม่ OCR) ผูกกับรายการ · แนบผ่านแชตได้ | **จริง** |
| R5 | คำนวณถ่วงน้ำหนัก (milestone → รายการ → รวม) + planned vs actual + สถานะ | **จริง — โค้ดล้วน** |
| R6 ⭐ | **Agent ประเมินหลักฐาน** — ไล่อ่านเอกสารของรายการหนึ่ง เทียบเกณฑ์ **จัดชั้น A–D + ระบุว่ายังขาดอะไร** (เป็นข้อเสนอ) | **จริง** (§5.4) |
| R7 | **สองตัวเลขแยกกัน** — งานคืบหน้า / ยืนยันด้วยหลักฐาน แสดงคู่กันเสมอ | **จริง** |
| R8 | ร่างคำตอบจากหลักฐานที่ **ยืนยันแล้ว** + flag เมื่อข้อมูลไม่พอ | **จริง** (เรียกโมเดล) |
| R9 | คนตรวจ → ยืนยัน / **ตีกลับพร้อมเหตุผล → แก้ → เสนอใหม่** → ผู้บริหารอนุมัติ | **จริง** |
| R10 | **Dashboard ผู้บริหาร** — KPI คู่ · "สิ่งที่ต้องตัดสิน" ขึ้นก่อน | **จริง** |
| R11 | **เอกสารรายเดือน Word + PDF** ตามฟอร์มต้นแบบ + **ทะเบียนหมายเหตุ** + ตัวอ้างอิงทุกข้อความ | **จริง** |
| R12 | ข้อมูลคงอยู่หลัง restart backend | **จริง** |
| R13 | เกณฑ์อยู่ในตาราง `app_setting` **ห้าม hardcode** | **จริง** |
| R14 | `verify.py` — เคส AC-01 … AC-20 | **จริง** |

### 3.2 Optional — ถ้าเวลาเหลือ

O1 · **จับกองที่ยังไม่ส่ง + Outbox ร่างข้อความ (คนกดส่ง)** — *ย้ายลงมาจาก Required เมื่อ 15 ก.ย.
เพื่อเอาเวลาไปทำ R3 แชต · คุณค่าบางส่วนได้คืนผ่านการ์ด "สิ่งที่ต้องส่งรอบนี้" ในแชต (§5.4.1)*
O2 · แชตบอตถาม–ตอบสำหรับผู้บริหาร (อ่านอย่างเดียว ตอบพร้อมอ้างอิง)
O3 · กราฟ planned vs actual ย้อนหลัง
O4 · agent ตรวจความไม่สอดคล้องข้ามรายการ

### 3.3 Out of scope — พร้อมเหตุผล `[ยืนยันแล้ว]`

| ไม่ทำ | เพราะ |
|---|---|
| **นำเข้า Excel/CSV อัตโนมัติ** | ใช้ฟอร์มกรอกมือแทน — ตัดเวลา parse/validate ไปลงที่ agent ซึ่งเป็นหัวใจของโจทย์ |
| **ตัวชี้วัดสิ่งแวดล้อม (kWh · จำนวนรถ)** | เก็บไว้รอบหน้า — รอบนี้เน้นหลักฐานและคำตอบ |
| ส่งอีเมล / LINE / Teams จริง | ระบบร่างได้ แต่การส่งต้องให้คนกด — เวิร์กช็อปไม่ควรยิงข้อความถึงคนจริง |
| SSO / AD จริง | restricted · ใช้ปุ่มสลับบทบาท + บังคับสิทธิ์จริงใน backend แทน |
| เชื่อมระบบเดิม (SharePoint · HR) | ไม่มีสิทธิ์เข้าถึงในกรอบเวลานี้ |
| **คำนวณ CO₂ / Fuel Saving ตามมาตรฐาน GHG Protocol** | **ไม่มี emission factor ที่อ้างอิงได้** — ตัวเลขสิ่งแวดล้อมที่ไม่มีที่มาเสี่ยงกว่าการไม่แสดง |
| OCR เอกสารสแกน / รูปถ่าย | นอกเวลาที่มี — เป็น known limitation ที่ระบุไว้ชัด |
| Deploy ขึ้น cloud | endpoint โมเดลเป็น IP ภายใน **ต้องรันในเครือข่ายเดียวกัน** |
| RAG / vector database | ข้อมูลรอบนี้ใส่ context ตรง ๆ ได้ — เพิ่มความซับซ้อนโดยไม่แก้ปัญหาอะไร |
| **ตัดสินว่า "ผ่านการประเมินแล้ว"** | **ไม่ใช่อำนาจของใครในระบบ** — เป็นของผู้ตรวจ ระบบบอกได้แค่ว่าหลักฐานพร้อมหรือยัง |

---

## 4. Workflow, screens and visual design

### 4.1 ลำดับงานหนึ่งรอบ

| # | Actor | Input | Action | Output | Exception |
|:---:|---|---|---|---|---|
| 1 | **Agent ผู้ช่วย** | บทบาท + รอบปัจจุบัน | เปิดแชต → ขึ้น**การ์ด "สิ่งที่ต้องส่งรอบนี้"** + กำหนดส่ง | รายการงานในแชต | ไม่มีงานค้าง → บอกตามจริง ไม่ใช่หน้าเปล่า · โมเดลล่ม → ถอยไปหน้าฟอร์ม |
| 2a | เจ้าของข้อมูล | ข้อความ เช่น *"อัปเดต A-05 เป็น 60%"* | พิมพ์ในแชต | — | — |
| 2b | **Agent ผู้ช่วย** ⭐ | ข้อความ + รายการของกองนั้น | ตีความเป็น `draft_progress(...)` → **ขึ้นการ์ดยืนยัน** แสดงค่าเดิม → ค่าใหม่ | แถว **pending** เท่านั้น | **ตีความไม่ชัด (เช่น "เกือบเสร็จแล้ว") → ถามกลับ ห้ามเดา ห้ามขึ้นการ์ด** · อ้างถึงรายการของกองอื่น → ปฏิเสธ |
| 2c | เจ้าของข้อมูล | การ์ดยืนยัน | **กดยืนยัน** (หรือแก้ค่าก่อนกด) | บันทึกค่าจริง + `audit_log` **actor = ผู้ใช้** | ค่านอกช่วง 0–100 / วันที่ไม่ถูกต้อง → ชี้จุดที่ผิดบนการ์ด **ไม่บันทึก** · ไม่กด → ไม่มีอะไรเปลี่ยน (AC-17) |
| 3 | เจ้าของข้อมูล | ไฟล์หลักฐาน | ลากไฟล์เข้าแชต ผูกกับรายการ | `evidence` สถานะ "รอประเมิน" | ชนิดไฟล์ไม่รองรับ/ขนาดเกิน → ปฏิเสธพร้อมเหตุผล · **ไม่มีวันที่ในเอกสาร → ขึ้นการ์ดถามวันที่ ห้ามเดาจากวันอัปโหลด** |
| 4 | **โค้ด** | ข้อมูลที่บันทึกแล้ว | คำนวณถ่วงน้ำหนักขึ้นทุกชั้น + planned vs actual + สถานะ | `progress` · `status` | น้ำหนักรวมไม่เท่า 100 → เตือนและไม่ให้บันทึก |
| 5 | **Agent** ⭐ | รายการ + เกณฑ์ + เอกสารทั้งหมดของรายการนั้น | ไล่อ่านทีละชิ้น เทียบเกณฑ์ **จัดชั้น A–D + เหตุผล + สิ่งที่ยังขาด** | ข้อเสนอสถานะ **pending** | ชนเพดาน tool call → รายงานว่าประเมินไม่ครบ · JSON ผิดรูป 2 ครั้ง → "ประเมินไม่สำเร็จ" ให้คนจัดชั้นเอง |
| 6 | ทีมกลาง | ข้อเสนอ + เอกสาร | ยืนยันชั้น หรือแก้พร้อมเหตุผล | `confirmed_at` → **นับเข้าตัวเลข Verified** | ไม่ทำอะไร → ค้างสถานะ "รอตรวจ" |
| 7 | **โมเดล** | หลักฐานที่ **ยืนยันแล้ว** + เกณฑ์ | ร่างคำตอบอ้างอิงหลักฐาน · flag "ข้อมูลไม่พอ" พร้อมระบุว่าขาดอะไร | `response` สถานะ **draft** | ไม่มีหลักฐานชั้น A/B เลย → **ไม่ร่าง** แจ้งว่าต้องเก็บหลักฐานก่อน |
| 8 | ทีมกลาง | ร่างคำตอบ | ตรวจ แก้ ส่งขออนุมัติ | สถานะ "รออนุมัติ" | — |
| 9 | ผู้บริหาร | คำตอบ + หลักฐาน | อนุมัติ **หรือตีกลับพร้อมเหตุผล** | อนุมัติแล้ว / กลับไปขั้น 8 | — |
| 10 | **โค้ด** | ทุกอย่างที่อนุมัติแล้ว | ประกอบเอกสารรายเดือน Word/PDF **+ ทะเบียนหมายเหตุ** | ไฟล์ส่งออก | **ข้อความที่ไม่มีตัวอ้างอิง → สร้างไม่ผ่าน โยน error** |
| ~~11~~ | ~~โค้ด~~ | ~~กำหนดส่ง · วันอัปเดตล่าสุด~~ | ~~จับกองที่ยังไม่ส่ง → ร่างข้อความเข้า Outbox~~ | **ย้ายไป Optional O1** (§3.2) | — |
| ~~12~~ | ~~ทีมกลาง~~ | ~~Outbox~~ | ~~ตรวจข้อความแล้วกดส่งเอง~~ | **ย้ายไป Optional O1** | — |

> **ขั้น 2a–2c คือหัวใจของกติกา** — agent หยุดที่การ์ด **การเขียนค่าเกิดจากการกดของคน**
> `audit_log.actor` ต้องเป็น user id เสมอ **ห้ามเป็น `ai`** (§5.5 · AC-18)

### 4.2 กติกาหัวใจ — สามคอลัมน์ที่ห้ามรวมกัน

```
   ความคืบหน้างาน       หลักฐาน              การยืนยัน
   (เจ้าของกรอก)        (agent จัดชั้น)       (คนอนุมัติ)
   ─────────────        ─────────────        ─────────────
   8/10 milestone       2 ชิ้น · ชั้น C ทั้งคู่   ยังไม่ยืนยัน
        80%                  ยังไม่นับ            พร้อม 0%

   ▲ ตัวเลขนี้ไม่ดันตัวเลขขวาขึ้นได้เลย ─────────────┘
```

> **ระบบไม่แปลง % งานเสร็จ เป็นความพร้อมโดยอัตโนมัติ** — เป็นเคสทดสอบ **AC-01**

### 4.3 หน้าจอ `[เสนอ]`

```text
Layout ร่วม
  Header : ชื่อระบบ | ปุ่มสลับบทบาท [เจ้าของข้อมูล ▾ / ทีมกลาง / ผู้บริหาร]
           | 🔔 Outbox | แถบ "ข้อมูลตัวอย่าง (Mockup)" ติดไว้ตลอด ห้ามซ่อน
  Sidebar: หน้าแรก / แชตกับผู้ช่วย / รายการทั้งหมด / ศูนย์ตรวจสอบ / เอกสารรายเดือน

1) หน้าแรก — Dashboard ผู้บริหาร (เปิดมาเจอก่อน)
   แถว 1: 📋 สิ่งที่ต้องตัดสิน   ← ขึ้นก่อนตัวเลขทั้งหมด
            ⬜ A-02  งานเสร็จ 100% · หลักฐาน 0 ชิ้นที่นับได้
            🛑 A-04  หลักฐานขัดกับ A-08
            ⚠️ กอง C ยังไม่ส่งข้อมูลรอบนี้ (เลยกำหนด 6 วัน)
   แถว 2: KPI คู่  [งานคืบหน้า 68%]  [ยืนยันด้วยหลักฐาน 41%]  [รอตรวจ 5] [ยังไม่ส่ง 3]
   แถว 3: ตารางทุกรายการ | ชื่อ | กอง | น้ำหนัก | งานคืบหน้า | ยืนยันแล้ว | สถานะ | อัปเดตล่าสุด
   แถว 4: ปุ่ม [สร้างเอกสารรายเดือน]

2) แชตกับผู้ช่วย (เจ้าของข้อมูล)  ← หน้าหลักของบทบาทนี้
   บน   : ชื่อกอง · รอบปัจจุบัน · ลิงก์เล็ก ๆ "ใช้ฟอร์มแทน" (ทางสำรองเมื่อโมเดลล่ม)
   กลาง : บทสนทนา + การ์ด 4 แบบ
            ① สิ่งที่ต้องส่งรอบนี้   — รายการ + กำหนดส่ง + รายการที่ถูกตีกลับพร้อมเหตุผล
            ② ยืนยันความคืบหน้า     — A-05 · M1 · 40% → 60% · วันที่  [ยืนยัน] [แก้]
            ③ ผลประเมินหลักฐาน      — ชั้นที่ agent เสนอ + เหตุผล + "ยังขาดอะไร"  [แนบเพิ่ม]
            ④ กรอกวันที่เอกสาร      — เมื่อสกัด document_date ไม่ได้ (AC-03)
   ล่าง : ช่องพิมพ์ + ปุ่มแนบไฟล์ (ลากวางได้)
   ⚠ การ์ด ② คือจุดที่ agent หยุด — ค่าจริงเปลี่ยนตอนคนกดเท่านั้น

3) รายละเอียดรายการ  ← หน้าที่สำคัญที่สุด
   ซ้าย  : ความคืบหน้า + milestone (แก้ได้) · แถบเลื่อน % + วันที่จริง
   กลาง  : หลักฐานที่แนบ + ชั้น A/B/C/D ที่ agent เสนอ + เหตุผล + ปุ่มยืนยัน/แก้
   ขวา   : ✅ "ยังขาดอะไร" เป็นรายการติ๊กได้
   ล่าง  : ประวัติการแก้ / การตีกลับ พร้อมเหตุผล

4) ศูนย์ตรวจสอบ (ทีมกลาง)
   แท็บ A "รอยืนยันชั้นหลักฐาน" : ข้อเสนอของ agent → ยืนยัน / แก้พร้อมเหตุผล
   แท็บ B "ร่างคำตอบรอตรวจ"   : ป้าย "ข้อมูลไม่พอ" ถ้ามี → แก้ → ส่งขออนุมัติ
   แท็บ C "ยังไม่ส่ง"          : แยกตามกอง + จำนวนวันที่เลยกำหนด → [สร้างร่างข้อความ]
   แท็บ D "Outbox"            : ร่างข้อความ + ปุ่มส่ง (คนกดเอง)

5) เอกสารรายเดือน
   รายการคำตอบ + สถานะ (ร่าง / รออนุมัติ / อนุมัติแล้ว / ตีกลับ)
   ปุ่ม [Export Word] [Export PDF]
   ⚠️ ท้ายเอกสาร: ทะเบียนหมายเหตุ — รายการที่ยังไม่มีหลักฐานรองรับ ห้ามซ่อน
```

### 4.4 การออกแบบภาพ `[เสนอ — รอยืนยัน]`

- **อุปกรณ์/ภาษา:** laptop เป็นหลัก (1440×900 รองรับลงถึง 1024) · หน้าผู้บริหารอ่านบนมือถือได้ · UI ภาษาไทย ศัพท์เทคนิคคงภาษาอังกฤษ · **เอกสารส่งออกภาษาอังกฤษตามฟอร์มต้นแบบ**
- **ชั้นหลักฐานใช้สัญลักษณ์ ไม่ใช่สีอย่างเดียว:** A ◆ / B ◇ / C ○ / D ✕
- **contrast ≥ 4.5:1** · ตัวอักษรในตารางไม่เล็กกว่า 14px · ตัวเลขในตารางใช้ tabular figures
- **สถานะที่ต้องออกแบบครบ:** ยังไม่มีข้อมูล · กำลังโหลด · **agent กำลังประเมิน (พร้อมปุ่มยกเลิก)** · สำเร็จ · ข้อมูลขาด/ผิด · **โมเดลล้มเหลว** · รอตรวจ · ถูกตีกลับพร้อมเหตุผล · อนุมัติแล้ว
- **ห้าม:** hero banner · gradient ฟุ้ง · **ตัวเลขที่ไม่ได้มาจากฐานข้อมูล** · chat ที่ไม่มีหน้าที่ในเวิร์กโฟลว์
- ⚠️ **ยังไม่มีชุดสี/โลโก้อ้างอิงขององค์กร** `[คำถามค้าง]` — ใช้ธีมเรียบ เขียว-เทาไปก่อน

---

## 5. AI behavior and boundaries

### 5.1 อะไรคือโค้ด อะไรคือโมเดล อะไรคือ agent

| งาน | เป็นอะไรจริง ๆ |
|---|---|
| คำนวณถ่วงน้ำหนัก · planned vs actual · นับวันเลยกำหนด · สิทธิ์ · validate ฟอร์ม · audit log · ประกอบเอกสาร | ❌ **โค้ดธรรมดา** — แม่นกว่าและตรวจย้อนกลับได้ |
| ร่างคำตอบจากหลักฐานที่ยืนยันแล้ว ตามโครงของฟอร์ม | 🔶 **fixed workflow ที่มีขั้นตอน AI** — โค้ดคุมลำดับ |
| **ประเมินว่าหลักฐานของรายการหนึ่งพอหรือยัง** — ต้องไล่ดูหลายชิ้น เทียบเกณฑ์ ตัดสินว่าพอ/ยังขาด แล้วเลือกว่าจะอ่านอะไรต่อ | ✅ **agent จริง** (§5.4) |
| **รับอัปเดตจากกองผ่านแชต** — ตีความว่าผู้ใช้หมายถึงรายการไหน milestone ไหน ค่าเท่าไร · ต้องดูรายการของกองนั้นก่อนจึงจะตีความได้ · ไม่ชัดต้องถามกลับ | ✅ **agent จริง ตัวที่สอง** (§5.4.1) |

> **แกนของระบบ:** *ตัวเลขทุกตัวมาจากโค้ด ไม่ได้มาจากโมเดล และตรวจย้อนกลับได้ทุกตัว*
> agent ตัดสินว่า **หลักฐานพอหรือยัง** — ไม่เคยตัดสินว่า **คะแนนเท่าไร**

### 5.2 สิ่งที่โมเดลห้ามทำ — บังคับด้วยการไม่มี tool

```
ไม่มีฟังก์ชันเหล่านี้อยู่ในโค้ดเลย:
   set_weight · confirm_tier · confirm_completion · set_status
   send_notification · approve_response · export_final · edit_plan · delete_*
   write_progress · commit_update          ← เพิ่มเมื่อทำแชต (§5.4.1)
```

> ข้อห้ามใน prompt พิสูจน์กับผู้ตรวจไม่ได้ แต่ **โค้ดที่ไม่มีฟังก์ชันนั้นอยู่เลย เปิดไฟล์ให้ดูได้**

### 5.3 เงื่อนไขหยุด

- **ไม่มีหลักฐานชั้น A หรือ B เลย → ไม่ร่างคำตอบ** และความพร้อมไม่ขึ้น (ไม่ใช่ให้คะแนนต่ำ — ไม่ให้เลย)
- ข้อมูลไม่พอ → **หยุด + ระบุว่าขาดอะไรถึงจะพอ** เป็นรายการ ไม่ใช่ปฏิเสธลอย ๆ
- เอกสารอ่านไม่ออก / ไม่มีวันที่ในตัวเอกสาร → **ถาม ห้ามเดาจากวันอัปโหลด**
- ไม่มีข้อมูลในระบบ → ตอบว่าไม่มี **ห้ามแต่งตัวเลขหรือชื่อ**
- **ตีความคำสั่งจากแชตไม่ชัด → ถามกลับ ห้ามเดาค่า** (เช่น "เกือบเสร็จแล้ว" → ถามว่ากี่เปอร์เซ็นต์)
- JSON ผิดรูป 2 ครั้งติด → หยุด บันทึกว่าไม่สำเร็จ **แสดงบนหน้าจอตามจริง**
- ⚠️ โมเดล local ขนาดเล็กเลือก tool ได้แย่กว่าโมเดลใหญ่ → **ลูปต้องตื้น tool ต้องน้อย โค้ดคุมขอบเขต**
  · **agent สองตัวใน §5.4 และ §5.4.1 ต้องใช้คนละชุด tool ห้ามรวมเป็นตัวเดียว**

### 5.4 Agent ตัวที่หนึ่ง — ประเมินหลักฐาน ⭐

| เรื่อง | ค่า |
|---|---|
| **เป้าหมาย** | ตอบว่า *"หลักฐานของรายการนี้พอสำหรับเกณฑ์หรือยัง และถ้ายัง ขาดอะไร"* |
| **State** | รายการที่กำลังประเมิน · เกณฑ์ของรายการนั้น · เอกสารที่อ่านไปแล้ว · ข้อสรุประหว่างทาง |
| **Tool อ่านอย่างเดียว** | `get_item(item_id)` → ชื่อ เกณฑ์ น้ำหนัก สถานะ<br>`list_evidence(item_id)` → รายการเอกสาร + metadata<br>`read_evidence(evidence_id)` → ข้อความที่สกัดแล้ว (ตัดที่ N อักษร)<br>`get_previous_review(item_id)` → ผลการประเมินรอบก่อน + เหตุผล |
| **Tool เขียนได้** | `propose_tier(evidence_id, tier, reason)` → เขียนลงตาราง **pending**<br>`propose_gap(item_id, missing[])` → เขียนลงตาราง **pending** |
| **ห้ามมี** | เขียน/แก้ข้อมูลจริง · ยืนยันเอง · ส่งข้อความ · เปลี่ยนน้ำหนักหรือสถานะ · export |
| **เพดาน** | **ไม่เกิน 10 tool call ต่อรายการ · 60 วินาที** · ประเมินทีละรายการ ไม่ให้วิ่งข้ามเอง |
| **เงื่อนไขหยุด** | ได้ข้อสรุปครบทุกเกณฑ์ของรายการนั้น **หรือ** ชนเพดาน (แล้วรายงานว่าประเมินไม่ครบ ไม่ใช่เดาให้จบ) |
| **จุดตรวจของคน** | ทีมกลางยืนยันทุกข้อเสนอก่อนมีผล — `propose_*` ไม่เปลี่ยนตัวเลขใด ๆ จนกว่าจะ confirm |

**การ implement — สองทาง ขึ้นกับผลทดสอบ TF6** `[เสนอ]`

| | เงื่อนไข | วิธี |
|---|---|---|
| **ทางหลัก** | TF6 ผ่าน — endpoint คืน `tool_calls` จริง | ใช้ native tool calling · validate arguments ทุกครั้งก่อน execute · คืนผลกลับเข้าโมเดล |
| **ทางสำรอง** | TF6 ไม่ผ่าน | **ลูปเลือก action ด้วย JSON ที่ validate ด้วย schema ในโค้ด** — โมเดลคืน `{"action":"read_evidence","args":{...}}` → โค้ด validate → execute → ป้อนผลกลับเข้าลูป |

> ทางสำรองยัง**เป็น agent ตามนิยาม** (โมเดลเลือกการกระทำจากผลที่สังเกตได้ ภายใต้ขอบเขตที่บังคับ)
> เพียงแต่ไม่พึ่ง native tool-calling API · **ถ้าใช้ทางสำรอง ต้องระบุตามจริงบนสไลด์ ห้ามอ้างว่าใช้ native tool calling**
>
> ✅ **TF6 ผ่านแล้ว (§8) → ใช้ทางหลัก** · ทางสำรองเก็บไว้เผื่อ endpoint เปลี่ยน

### 5.4.1 Agent ตัวที่สอง — ผู้ช่วยส่งข้อมูลรายกอง ⭐

อยู่หน้าเจ้าของข้อมูล (§4.3 หน้า 2) · **คนละชุด tool กับ §5.4** ตามข้อเตือนใน §5.3

| เรื่อง | ค่า |
|---|---|
| **เป้าหมาย** | ช่วยเจ้าของข้อมูลส่งความคืบหน้าและหลักฐานให้ครบ และ**บอกทันทีว่ายังขาดอะไร** — ย่นลูปป้อนกลับจากหลายวันเหลือไม่กี่วินาที |
| **State** | กองของผู้ใช้ · รอบปัจจุบัน · รายการที่ยังไม่ส่ง · ข้อความก่อนหน้าในบทสนทนา |
| **Tool อ่านอย่างเดียว** | `list_my_items()` → รายการของกองผู้ใช้ + กำหนดส่ง + สถานะ<br>`get_item(item_id)` → milestone และค่าปัจจุบัน<br>`get_gaps(item_id)` → "ยังขาดอะไร" ที่ §5.4 เสนอไว้<br>`list_evidence(item_id)` → เอกสารที่แนบแล้ว + ชั้น |
| **Tool เขียนได้ (pending เท่านั้น)** | `draft_progress(item_id, milestone_seq, percent, actual_date)` → **ขึ้นการ์ดยืนยัน**<br>`request_evidence_date(evidence_id)` → ขึ้นการ์ดกรอกวันที่ (AC-03) |
| **ห้ามมี** | `write_progress` · `commit_update` · เขียนค่าจริงใด ๆ · ยืนยันชั้นหลักฐาน · แตะรายการของกองอื่น · ส่งข้อความ · export |
| **เพดาน** | **ไม่เกิน 6 tool call ต่อหนึ่งข้อความของผู้ใช้** · ใช้ `app_setting.agent_max_tool_calls` ร่วมกันได้ |
| **เงื่อนไขหยุด** | ขึ้นการ์ดได้ **หรือ** ถามกลับเมื่อตีความไม่ชัด — **ห้ามเดาค่าให้จบ** |
| **จุดตรวจของคน** | **เจ้าของข้อมูลกดยืนยันการ์ดเอง** · `draft_progress` ไม่เปลี่ยนค่าจริงจนกว่าจะกด (AC-17) |

```
พิมพ์/แนบไฟล์  →  agent ตีความ  →  การ์ดแสดงสิ่งที่จะเปลี่ยน  →  คนกดยืนยัน  →  โค้ดเขียนฐาน
                                     ▲                          ▲
                     agent หยุดตรงนี้ ┘      การเขียนเป็นการกระทำของคน ┘  audit_log.actor = user
```

> **ทำไมต้องมีการ์ด ไม่ให้ agent เขียนตรง ๆ** — §5.5 ระบุว่า AI กรอกความคืบหน้าไม่ได้
> การ์ดทำให้ผู้ใช้เห็นค่าที่โมเดลตีความได้**ก่อน**มันมีผล ซึ่งกันทั้งเรื่องอำนาจและเรื่องตีความภาษาไทยผิด
> และถ้าโมเดลล่ม ฟอร์มเดิมยังใช้กรอกได้ตามปกติ (AC-12)

### 5.5 ตารางอำนาจ

| การกระทำ | AI | เจ้าของข้อมูล | ทีมกลาง | ผู้บริหาร |
|---|:---:|:---:|:---:|:---:|
| เสนอชั้นหลักฐาน / ระบุสิ่งที่ขาด / ร่างคำตอบ | ✅ | 💬 ท้วงได้ | ✅ | ❌ |
| กรอกความคืบหน้า / แนบหลักฐานของกองตัวเอง | ✏️ **ร่างการ์ดให้** | ✅ *(กดยืนยันเอง)* | ✅ | ❌ |
| **ยืนยันชั้นหลักฐาน → นับเข้าตัวเลข Verified** | ❌ | ❌ | ✅ | ❌ |
| แก้ข้อมูลของกองอื่น | ❌ | ❌ | ✅ *(บันทึก + แจ้งเจ้าของ)* | ❌ |
| **กำหนดน้ำหนัก / เกณฑ์ในหน้าตั้งค่า** | ❌ | ❌ | ✅ *(บันทึกทุกครั้ง)* | ❌ |
| **เปลี่ยนสถานะ On Track / At Risk / Delayed** | ❌ | ❌ | ❌ | ❌ ← **โค้ดคำนวณ ไม่มีใครตั้งเอง** |
| ร่างข้อความแจ้งเตือน | ✅ | ❌ | ✅ | ❌ |
| **กดส่งข้อความ** | ❌ | ❌ | ✅ | ❌ |
| ตีกลับพร้อมเหตุผล | ❌ | ❌ | ✅ | ✅ |
| **อนุมัติคำตอบ / สั่งสร้างเอกสารฉบับจริง** | ❌ | ❌ | ❌ | ✅ |
| **ประกาศว่าผ่านการประเมิน** | ❌ | ❌ | ❌ | ❌ ← **ไม่ใช่อำนาจของใครในระบบ** |

> **✏️ ร่างการ์ดให้ ≠ เขียนได้** — agent ใน §5.4.1 ตีความคำสั่งแล้วขึ้นการ์ด แต่ค่าจริงเปลี่ยนตอนคนกดเท่านั้น
> `audit_log.actor` ของการเปลี่ยนค่าต้องเป็น **user id เสมอ ห้ามเป็น `ai`** (AC-18)

### 5.6 กฎการอ้างอิงในเอกสารรายเดือน

| กรณี | เอกสารต้องเขียนว่าอย่างไร |
|---|---|
| มีหลักฐานชั้น A/B | ข้อความ + **ตัวอ้างอิง `[E-014 น.3]`** คลิกเปิดต้นทางได้ |
| มีแต่ชั้น C (ร่าง/แผน) | ระบุว่า *"อยู่ระหว่างดำเนินการ"* + ตัวอ้างอิง + **หมายเหตุว่ายังไม่นับ** |
| **ไม่มีหลักฐานเลย (ชั้น D)** | 🔴 **ห้ามข้ามไปเงียบ ๆ** — ต้องขึ้นในทะเบียนหมายเหตุ พร้อมเจ้าของและวันอัปเดตล่าสุด |
| ข้อความที่ AI เรียบเรียงเอง | ติดป้าย **`[สังเคราะห์จาก E-003, E-011]`** ไม่ปล่อยเป็นประโยคลอย |
| **ข้อความที่ไม่มีตัวอ้างอิง** | **สร้างเอกสารไม่ผ่าน — โยน error** ไม่ใช่เตือนแล้วปล่อยผ่าน (AC-10) |

> **ทุกฉบับต้องมี "ทะเบียนหมายเหตุ" ท้ายเอกสาร** — สิ่งที่ไม่มีหลักฐานคือสิ่งที่หายจากรายงานได้ง่ายที่สุด
> **การบังคับให้มันปรากฏ คือคุณค่าหลักของระบบนี้**

---

## 6. Data and storage

### 6.1 Source inventory

| แหล่งข้อมูล | รูปแบบ | เข้าระบบอย่างไร | สถานะ | Fallback ที่ตกลงกัน |
|---|---|---|---|---|
| 12 รายการ (ข้อประเมิน + โครงการ) + เกณฑ์ + น้ำหนัก | seed script | โหลดตอนเริ่ม / ปุ่มในแอป | **available** (synthetic) | เป็น fallback อยู่แล้ว — ติดป้ายในแอปทุกหน้า |
| เอกสารหลักฐานตัวอย่าง ~24 ชิ้น | .md / .pdf | seed + อัปโหลด | **available** (synthetic) | ปั้นข้อบกพร่องใส่ไว้ตั้งใจ (§6.2) |
| แบบฟอร์มคำตอบต้นแบบ | .docx | นำเข้าเป็น seed | **available** | — |
| ความคืบหน้ารายเดือนจริงจากกอง | — | ฟอร์มกรอกมือ | **missing** | กรอกจากข้อมูลตัวอย่าง |
| เกณฑ์การประเมินฉบับจริง | เอกสาร | ตั้งค่าตอนเริ่ม | **restricted** | เกณฑ์สมมติที่เขียนขึ้นเอง ติดป้ายทุกหน้า |
| ข้อมูลเที่ยวบิน / emission factor | — | — | **missing** | **ตัดการคำนวณ CO₂ ออกจากขอบเขต** |
| SharePoint / AD / SSO / SMTP | — | — | **restricted** | role switcher · Outbox ในฐานข้อมูล ไม่ส่งจริง |
| Endpoint โมเดล | HTTP | backend เรียก | ✅ **available · ทดสอบแล้ว** | DGX `10.0.63.215` · ไม่ต้อง auth · TF1–TF8 ผ่านครบ (§8) |

> **การส่งข้อมูลให้โมเดล:** ข้อมูลทั้งหมดเป็น synthetic ที่สร้างขึ้นเอง **ไม่มีข้อมูลจริงขององค์กร**
> จึงส่งให้ endpoint ของเวิร์กช็อปได้ — ปิดความเสี่ยงเรื่องข้อมูลองค์กรทั้งหมด

### 6.2 ชุดข้อมูลตัวอย่าง — ปั้นข้อบกพร่องใส่ก่อนเขียนโค้ด

| รายการ | ชนิด | ชั้นที่ตั้งใจให้เป็น | ประเด็นที่ซ่อนไว้ | เคสที่ทดสอบ |
|---|---|---|---|---|
| **A-02** | ข้อประเมิน | C, D | **milestone เสร็จ 10/10 แต่ไม่มีหลักฐานที่นับได้** | **AC-01** ⭐ |
| **A-03** | ข้อประเมิน | A, B, C | รายงานผลวัดที่ลงนามแล้ว — เส้นทางที่สำเร็จ | AC-04 |
| **A-04** | ข้อประเมิน | C, D | อ้างว่า "ครบถ้วน" แต่ขัดกับ A-08 | AC-08 |
| **A-05** | ข้อประเมิน | C | หลักฐานเดียวคือ **"(ร่าง) แผนดำเนินการ"** | **AC-02** ⭐ |
| **A-07** | โครงการ | B, C | **ล่าช้ากว่าแผนมาก** (ควรได้ ~80% ทำได้ 35%) · ขอเลื่อนครั้งที่ 3 | AC-11 |
| **A-08** | โครงการ | A, B | หลักฐานชั้น A **ระบุความครอบคลุมเพียง 60%** → ขัดกับคำกล่าวอ้างของ A-04 | AC-08 |
| **A-09** | โครงการ | B, ล้าสมัย | เอกสารลงวันที่ **14 เดือนก่อน** | AC-09 |
| **A-11** | โครงการ | — | เอกสาร **ไม่มีวันที่ในตัวเอกสาร** | AC-03 |
| **A-12** | ข้อประเมิน | — | **ไม่มีข้อมูลเลยในระบบ** | AC-05 |

> **ชุดนี้คือ golden set** — ปั้นข้อบกพร่องลงในข้อมูลก่อน แล้วค่อยเขียนโค้ดให้จับได้
> ไม่ใช่เขียนเทสต์ให้ผ่านโค้ดที่มีอยู่

### 6.3 Entities

```
division(id, name)
app_user(id, name, division_id, role[submitter|central|executive])

tracked_item(id, code, name, item_type[assessment_item|project],
             division_id, owner_user_id, weight, criteria_text,
             progress_cache, verified_cache, status_cache, due_date, updated_at)
milestone(id, item_id, seq, name, weight, planned_start, planned_end,
          actual_start, actual_end, percent_complete, status)

evidence(id, item_id, file_name, stored_path, uploaded_by, upload_date,
         document_date)          ← วันที่ในตัวเอกสาร · ว่างได้ → ระบบต้องถาม ห้ามเดา
evidence_tier(evidence_id, tier[A|B|C|D], reason, proposed_by[ai|user],
              confirmed_by, confirmed_at)   ← ยังไม่ confirmed = ยังไม่นับเข้า Verified
item_gap(id, item_id, missing_text, proposed_by, resolved_at)

response(id, item_id, period, text, status[draft|pending|approved|rejected],
         insufficient_flag, updated_by, updated_at)
response_claim(response_id, seq, text, evidence_ids_json,
               kind[cited|synthesized|remark])   ← ไม่มีแถวนี้ = export ไม่ได้
review_log(id, item_id, target[tier|response], action[approve|reject],
           reason, by_user, at)
agent_run(id, item_id, started_at, tool_calls_json, outcome, ended_at)  ← ตรวจย้อนกลับได้

outbox(id, to_display, subject, body, kind, created_at, approved_by, sent_status)
audit_log(id, actor, action, entity_type, entity_id, before, after, at)  ← เขียนอย่างเดียว
app_setting(key, value, description, updated_by, updated_at)             ← ห้าม hardcode
```

**กฎของน้ำหนัก:** ทุกระดับต้องรวมได้ 100 · แก้ค่าหนึ่ง → ระบบปรับสัดส่วนที่เหลือแล้ว **ให้ยืนยันอีกครั้ง** ·
รวมไม่ถึง 100 → ไม่ให้บันทึก · ทุกการแก้เข้า `audit_log` พร้อมค่าก่อน/หลัง

**ค่าใน `app_setting`:** `at_risk_threshold_points` = 30 · `alert_lead_days` = 15 ·
`monthly_due_day` = 5 · `evidence_stale_months` = 12 · `agent_max_tool_calls` = 10 ·
`agent_timeout_seconds` = 60 — **ทั้งหมดแก้ได้จากหน้าจอโดยไม่แตะโค้ด (AC-11)**

### 6.4 Storage

SQLite ไฟล์เดียวบนเครื่อง backend + โฟลเดอร์ `sample-data/uploads/` `[เสนอ]`
ผู้ใช้ไม่กี่คนบนเครื่องเดียว ไม่ต้องติดตั้ง database server · ขึ้นใช้งานจริงค่อยย้ายเป็น PostgreSQL โครงตารางเหมือนกัน
**ไม่ใช้ vector database** — ข้อมูลใส่ context ตรง ๆ ได้

---

## 7. Architecture and integrations

```text
  เบราว์เซอร์ (laptop)
        │  fetch JSON
        ▼
  Backend ── สิทธิ์ · validate · กฎน้ำหนัก · คณิตศาสตร์ทั้งหมด · audit log · ประกอบเอกสาร
        │                       │
        │ SQLite + ไฟล์แนบ       │ Agent loop — โค้ดคุมขอบเขต เพดาน และการ validate argument
        │                       ▼
        │              OpenAI-compatible  POST /v1/chat/completions
        │              ENV: LLM_BASE_URL · LLM_MODEL · LLM_API_KEY · LLM_TIMEOUT_MS
        │                       ├─▶ DGX Spark  (ทางหลัก)
        │                       └─▶ Qwen FPT   (สำรอง)
        ▼
  sample-data/ — 12 รายการ · เอกสาร ~24 ชิ้น
```

**Stack** `[เสนอ]` — เลือกให้ทำครบเส้นได้ในเวลาที่เหลือ และ key อยู่ฝั่ง server

| ชั้น | เลือกใช้ | เหตุผล |
|---|---|---|
| Frontend + Backend | **Next.js (App Router) + TypeScript** | UI และ API อยู่โปรเจกต์เดียว · เรียกโมเดลจาก server route → key ไม่หลุดไป browser |
| UI | Tailwind + shadcn/ui | ตาราง การ์ด ชิปสถานะ ฟอร์ม พร้อมใช้ |
| ฐานข้อมูล | SQLite + ORM (`better-sqlite3`) | ไฟล์เดียว ไม่ต้องติดตั้ง server |
| สร้างเอกสาร | `docx` + ตัวแปลง PDF | export ตามฟอร์มต้นแบบ |
| เรียกโมเดล | `fetch` ตรงไป OpenAI-compatible endpoint | ทั้งสอง endpoint ใช้สัญญาเดียวกัน สลับด้วย ENV |

**ข้อกำหนดร่วม**

- **API key อยู่ฝั่ง backend เท่านั้น** — ไม่อยู่ในหน้าเว็บ ไม่อยู่ใน log ไม่อยู่ในเอกสารนี้ ไม่อยู่ในแชท/สกรีนช็อต
- commit เฉพาะ `.env.example` ที่เป็น placeholder · ถ้า `LLM_BASE_URL` ว่างตอน start → ขึ้น configuration error ที่อ่านรู้เรื่อง
- **รันในเครือข่ายเดียวกับ endpoint — deploy cloud ไม่ได้** เพราะเป็น IP ภายใน
- ไม่เปิด port ของ endpoint ออกภายนอก และไม่ลดการป้องกันเครือข่ายเพื่อให้เดโมผ่าน
- **ทางเดินแบบไม่ใช้โมเดล (บังคับ — AC-12):** ปิด LLM แล้วระบบต้องยังเปิดได้ · กรอกได้ · แนบไฟล์ได้ · คำนวณได้ · แสดง dashboard ได้ · **ทีมกลางจัดชั้นหลักฐานเองได้** · แผง agent ขึ้นสถานะตามจริง

**ค่าที่ใช้จริง** — ยืนยันด้วยการทดสอบเมื่อ 15 ก.ย. 2569

| | DGX Spark (ทางหลัก) | Qwen FPT (สำรอง) |
|---|---|---|
| Base URL | `http://10.0.63.215:8000/v1` หรือ `…239` | `http://124.197.18.95:8000/v1` |
| Model | `nemotron-3.5-lightning` | `qwen3.8-27b` — **เอกสารระบุเองว่าลอกมาจากโน้ต ไม่ใช่ชื่อรุ่นทางการ** |
| ข้อควรระวัง | thinking เปิดโดยค่าเริ่มต้น — ต้องส่ง `chat_template_kwargs: {"enable_thinking": false}` | HTTP ธรรมดา — **ห้ามส่ง key หรือข้อมูลที่ไม่เปิดเผย** จนกว่าจะได้ HTTPS/เส้นทางภายในที่อนุมัติ |
| สถานะ | ✅ **ทีมได้เครื่องนี้ · ไม่ต้อง auth · ใช้งานได้จริง** | ⬜ ไม่ได้ใช้ — port · API path · model list · auth ยังไม่ยืนยัน |

---

## 8. Feasibility evidence

> ✅ **ทดสอบจริงแล้วเมื่อ 15 ก.ย. 2569 — ผ่านทั้ง 8 ข้อ** · ผลดิบ: `tools/endpoint-test-result.md`

| # | สิ่งที่ต้องพิสูจน์ | สถานะ |
|:---:|---|---|
| TF1 | `GET /health` ตอบจากเครื่อง backend | ✅ **ผ่าน** — `/v1/models` ตอบ HTTP 200 |
| TF2 | `GET /v1/models` มี model alias ตรงกับเอกสาร | ✅ **ผ่าน** — พบ `nemotron-3.5-lightning` (มี alias `lightning` ด้วย) |
| TF3 | `POST /v1/chat/completions` ได้คำตอบที่ตรงประเด็น ไม่ใช่แค่ JSON ถูกรูปแบบ | ✅ **ผ่าน** — ป้อน E-005 ถามขอบเขตการวัด ตอบ VTBS และ 42% ถูกต้อง · 3.8 วิ |
| TF4 | โมเดลตอบภาษาไทยได้คุณภาพใช้งานจริง | ✅ **ผ่าน** — ตอบไทยได้เต็มรูปแบบ |
| TF5 | **ป้อน "(ร่าง) แผนดำเนินการ" → ต้องได้ชั้น C และห้ามยกระดับ** | ✅ **ผ่าน** — ป้อน E-010 ได้ชั้น **C** เหตุผล: *"เป็นร่างแผนที่ยังไม่ได้รับการอนุมัติและไม่มีการดำเนินการจริง"* · 1.9 วิ |
| TF6 ⭐ | **`tool_calls` จริง** — เห็นการเลือก tool · validate arguments · คืนผลกลับเข้าโมเดล | ✅ **ผ่าน** — คืน `tool_calls` จริง เรียก `list_evidence({"item_id":"A-02"})` arguments ตรง schema · 0.5 วิ |
| TF7 | agent ทำงานครบ 1 ลูป (เลือก → เรียก → เห็นผล → ตัดสิน) ภายในเพดาน 10 call | ✅ **ผ่าน** — ป้อนผล tool กลับรอบสอง สรุปถูกต้อง · 0.7 วิ |
| TF8 | latency ของลูป agent อยู่ในระดับที่เดโมได้ | ✅ **ผ่าน** — วัดได้ 0.5–3.8 วิต่อ call (ค่าที่วัดจริง ไม่ใช่ประมาณการ) |

**ผลที่ตามมา** — ทดสอบเมื่อ **15 ก.ย. 2569** · เครื่องที่ได้รับมอบหมาย: **DGX 10.0.63.215** · **ไม่ต้องใช้ auth**

| | |
|---|---|
| **ใช้ทางหลักของ §5.4** | `tool_calls` ใช้ได้จริง → **ไม่ต้องใช้ทางสำรอง** |
| **กติกาชั้น C ยังต้องบังคับใน backend อยู่ดี** | TF5 ผ่านก็จริง แต่ผ่านจาก prompt — เพดานต้องอยู่ในโค้ดตาม §5.3 ไม่พึ่งว่าโมเดลจะทำถูกทุกครั้ง |
| **`.env` ที่ใช้จริง** | `LLM_BASE_URL=http://10.0.63.215:8000/v1` · `LLM_MODEL=nemotron-3.5-lightning` · ไม่มี key |

> ⚠️ **ขอบเขตของสิ่งที่ทดสอบไปแล้ว — อย่าอ่านเกินจริง**
> TF6/TF7 ทดสอบด้วย **tool เดียว ลูปสองรอบ** · agent จริงใน §5.4 มี **6 tool และเพดาน 10 call**
> ผ่านการทดสอบนี้ **ไม่ได้รับประกันว่าลูปเต็มจะทำงานถูก** โดยเฉพาะการเลือก tool ให้ถูกตัวเมื่อมีให้เลือกหลายตัว
> — ข้อนี้ต้องพิสูจน์อีกครั้งใน M3 ด้วย **AC-06**
>
> รายละเอียดดิบอยู่ใน `tools/endpoint-test-result.md` · รันซ้ำได้ด้วย `python tools/check_endpoint.py`

**ขั้นตอนที่รันไป (ทำซ้ำได้)**

1. ถาม facilitator สองคำถาม: **ได้เครื่องไหน** · **ต้องใช้ auth ไหม**
2. รัน TF1 → TF2 → TF3 ด้วยเอกสารจริงจากชุดข้อมูลตัวอย่าง
3. **TF5** — ป้อนเอกสาร "(ร่าง) แผนดำเนินการ" ของ A-05 → ต้องได้ชั้น C และห้ามยกระดับ
4. ป้อนเอกสารที่ไม่มีวันที่ (A-11) → ต้อง **ถาม** ไม่ใช่เดา
5. **TF6 ทดสอบแยกต่างหาก** — prompt ที่พูดถึง tool **ไม่ใช่** การเรียก tool · ต้องเห็น `tool_calls` จริง
6. **TF7** — รันลูปเต็มกับ A-03 (เคสที่ควรผ่าน) และ A-02 (เคสที่ควรหยุด)
7. **บันทึกผลตามจริงรวมข้อที่ไม่ผ่าน** — ห้ามเอาคำตอบที่เขียนไว้ล่วงหน้ามาแสดงแล้วบอกว่าต่อสำเร็จ

> ⚠️ ไฟล์ integration ทั้งสองเขียนตรงกันว่า **"อย่าสรุปว่า streaming / tool calling / JSON-schema output
> ใช้ได้ เพียงเพราะแชทธรรมดาใช้ได้"**
>
> **ถ้า TF5 ไม่ผ่าน** (โมเดลยกระดับ "ร่างแผน") → **ย้ายกติกาไปอยู่ในโค้ด ไม่ใช่ใน prompt**:
> บังคับเพดานชั้น C ในฝั่ง backend ไม่ว่าโมเดลจะเสนออะไรมา
> **ถ้า TF6 ไม่ผ่าน** → ใช้ทางสำรองใน §5.4 และ **ระบุตามจริงบนสไลด์**

---

## 9. Acceptance tests

| ID | เงื่อนไขเริ่มต้น / Input | ผลที่คาดหวัง | วิธีตรวจ |
|---|---|---|---|
| **AC-01** ⭐ | A-02 milestone เสร็จ 10/10 · ไม่มีหลักฐานชั้น A/B | **ตัวเลข "ยืนยันด้วยหลักฐาน" ต้องไม่ขึ้นเลย** · หน้าจอแสดงสองตัวเลขคู่กัน · ขึ้นในกล่อง "สิ่งที่ต้องตัดสิน" · **ไม่ร่างคำตอบ** | `verify.py` + ดูหน้าจอ |
| **AC-02** ⭐ | A-05 หลักฐานเดียวคือ "(ร่าง) แผนดำเนินการ" | agent จัดเป็นชั้น **C** · ความพร้อม**ไม่ขึ้น** · ข้อความ *"แผนไม่ใช่หลักฐานผลลัพธ์"* · **ระบุว่าต้องการหลักฐานแบบไหนเพิ่ม** | `verify.py` |
| **AC-03** | A-11 เอกสารไม่มีวันที่ในตัวเอกสาร | ระบบ **ถามหาวันที่** · **ห้ามเดาจากวันอัปโหลด** | `verify.py` |
| **AC-04** | A-03 แนบรายงานผลวัดที่ลงนามแล้ว | agent จัดเป็นชั้น **A** พร้อมเหตุผล → คนยืนยัน → ตัวเลข Verified ขยับ → ร่างคำตอบอ้างอิงเอกสารนั้น | ดูหน้าจอครบ 3 ขั้น |
| **AC-05** | ถามถึง A-12 ที่ไม่มีข้อมูลในระบบ | ตอบว่าไม่มีข้อมูลรองรับ · **ห้ามแต่งตัวเลขหรือชื่อ** | อ่านคำตอบ + ตรวจ log |
| **AC-06** ⭐ | สั่ง agent ประเมิน A-03 หนึ่งรอบ | `agent_run` บันทึกลำดับ tool call จริง · **เรียกเฉพาะ tool ที่อนุญาต** · ไม่เกิน 10 call · ข้อเสนอเข้าสถานะ **pending** ไม่เปลี่ยนตัวเลขใด ๆ | ตรวจ `agent_run` + เทียบตัวเลขก่อน/หลัง |
| **AC-07** | ตั้ง `agent_max_tool_calls` = 2 แล้วสั่งประเมิน A-03 ซึ่งมีเอกสาร 4 ชิ้น | agent **หยุดตามเพดาน** และ **รายงานว่าประเมินไม่ครบ** ไม่ใช่เดาให้จบ | ดูหน้าจอ + `agent_run` |
| **AC-08** | A-04 อ้างว่า "ครบถ้วน" ขณะที่ A-08 ระบุความครอบคลุม 60% | แจ้งความไม่สอดคล้อง **อ้างถึงหลักฐานทั้งสองฝั่ง** | `verify.py` |
| **AC-09** | A-09 เอกสารลงวันที่เกิน 12 เดือน | ลดชั้นเป็น **"ล้าสมัย"** อัตโนมัติ + แจ้ง | `verify.py` |
| **AC-10** | ร่างคำตอบมีข้อความที่ไม่มีตัวอ้างอิง | **สร้างเอกสารไม่ผ่าน — โยน error** ไม่ใช่เตือนแล้วปล่อย | `verify.py` |
| **AC-11** | แก้ `at_risk_threshold_points` จาก 30 เป็น 10 ในหน้าตั้งค่า | รายการที่เดิม On Track เปลี่ยนเป็น At Risk ทันที **โดยไม่ต้องแก้โค้ดหรือ restart** | แก้แล้วดู dashboard |
| **AC-12** | ตั้ง `LLM_BASE_URL` เป็น endpoint ที่ติดต่อไม่ได้ | ระบบยังเปิดได้ · กรอกได้ · แนบไฟล์ได้ · คำนวณได้ · **ทีมกลางจัดชั้นเองได้** · แผง agent ขึ้น error ที่กู้คืนได้ **ไม่มีคำตอบปลอม** | ตัด endpoint จริงแล้วลองใช้ |
| **AC-13** | ผู้ใช้กอง A ยิง request ตรงเพื่อแก้ข้อมูลกอง B | backend ตอบ **403** · ไม่มีข้อมูลรั่ว · บันทึก log | ยิง request ตรงแล้วดู response |
| **AC-14** | ผู้บริหารตีกลับคำตอบพร้อมเหตุผล | สถานะย้อนกลับไปทีมกลาง · เหตุผลแสดงชัด · แก้แล้วเสนอใหม่ได้ครบรอบ | ทดสอบ flow ตีกลับ–แก้–เสนอใหม่ |
| **AC-15** | อนุมัติครบแล้วสั่งสร้างเอกสารรายเดือน | ได้ Word + PDF ตามโครงฟอร์ม · ทุกข้อความมีตัวอ้างอิง · **A-02 ปรากฏในทะเบียนหมายเหตุ** | เปิดไฟล์แล้วเทียบกับฟอร์มต้นแบบ |
| **AC-16** | ปิด backend แล้วเปิดใหม่ | ข้อมูล · หลักฐาน · การยืนยัน · `agent_run` · `audit_log` ยังอยู่ครบ | restart จริงแล้วเทียบ |
| **AC-17** ⭐ | พิมพ์ในแชต "อัปเดต A-05 เป็น 60%" แล้ว **ไม่กดยืนยันการ์ด** | ค่าในฐาน **ไม่เปลี่ยน** · มีแถว pending อย่างเดียว · **ยังไม่มีรายการใน `audit_log`** | เทียบ DB ก่อน/หลัง |
| **AC-18** | กดยืนยันการ์ด | ค่าเปลี่ยนตามการ์ด · `audit_log.actor` = **user id ไม่ใช่ `ai`** · บันทึกค่าก่อน/หลัง | ตรวจ `audit_log` |
| **AC-19** | พิมพ์ "เกือบเสร็จแล้ว" (ไม่มีตัวเลข) | agent **ถามกลับว่ากี่เปอร์เซ็นต์** · **ไม่เดาค่า ไม่ขึ้นการ์ด** | อ่านคำตอบ |
| **AC-20** | ผู้ใช้กองสิ่งแวดล้อมพิมพ์ "อัปเดต A-07 เป็น 80%" (เป็นของกองวิศวกรรม) | agent ปฏิเสธเพราะไม่ใช่รายการของกองตัวเอง · ยิง request ตรง → backend ตอบ **403** | ทดสอบทั้งผ่านแชตและยิงตรง |

> **AC-01 · AC-02 · AC-06 คือหัวใจ** — สองข้อแรกพิสูจน์ว่าระบบแยก *คำกล่าวอ้าง* ออกจาก *หลักฐาน* ได้จริง
> ข้อที่สามพิสูจน์ว่า agent เป็น agent จริง ไม่ใช่ prompt ที่พูดถึง tool
> เคสเหล่านี้แสดงว่าระบบทำงานตามที่ตกลง **ไม่ได้พิสูจน์ความแม่นยำระดับ production**

---

## 10. Implementation sequence

| M | งาน | เสร็จเมื่อ | ครอบคลุม AC |
|:---:|---|---|---|
| **M0** | **ทดสอบ endpoint TF1–TF8** ก่อนแตะ UI · เลือกทางหลัก/ทางสำรองของ §5.4 | บันทึกผลจริงครบทุกช่อง รวมข้อที่ไม่ผ่าน | — |
| **M1** | โครงสร้างโปรเจกต์ + schema §6.3 + seed §6.2 + 3 บทบาท (บังคับสิทธิ์ backend) | เปิดเห็น 12 รายการ · สลับบทบาทแล้วแก้ของกองอื่นไม่ได้จริง | AC-13 |
| **M2** | ฟอร์มกรอกความคืบหน้า + แนบหลักฐาน + progress engine + สถานะ + dashboard KPI คู่ | ตัวเลขทั้งสองขึ้นจอพร้อมกัน ⏸ **จุดหยุดที่ 1** | AC-11 |
| **M3** ⭐ | **Agent loop** (§5.4) — tool · เพดาน · `agent_run` · ข้อเสนอเข้า pending + คนยืนยัน | **AC-01 · AC-02 · AC-06 ผ่าน** ⏸ **จุดหยุดที่ 2 — ของที่ชนะอยู่ในมือแล้ว** | AC-01 ⭐, AC-02 ⭐, AC-03, AC-04, AC-06 ⭐, AC-07 |
| **M4** | ร่างคำตอบ + วงจรตรวจ/ตีกลับ/อนุมัติ | วงจรครบรอบ | AC-05, AC-14 |
| **M5** | **เอกสารรายเดือน Word/PDF + ทะเบียนหมายเหตุ + บังคับตัวอ้างอิง** + ความไม่สอดคล้อง + เอกสารล้าสมัย | เปิดไฟล์ที่ export ได้จริง | AC-08, AC-09, AC-10, AC-15 |
| **M6′** ⭐ | **แชตผู้ช่วยส่งข้อมูล** (§5.4.1) — ChatShell + agent ตัวที่สอง + การ์ด 4 แบบ + การ์ดยืนยัน | **AC-17 · AC-18 · AC-19 · AC-20 ผ่าน** | AC-17 ⭐, AC-18, AC-19, AC-20 |
| ~~M6~~ | ~~กองที่ยังไม่ส่ง + Outbox~~ → **ย้ายไป Optional O1** (§3.2) เมื่อ 15 ก.ย. เพื่อเอาเวลามาทำ M6′ | — | — |
| **M7** | `verify.py` ครบทุกเคส + ทดสอบ restart + ทดสอบปิด endpoint + ซ้อมเดโม | ⏸ **จุดหยุดที่ 3 — พร้อมพิตช์** | AC-12, AC-16 |

**กฎ:** ทุก milestone ต้องจบด้วยของที่เดโมได้ · **เที่ยง 16 ก.ย. หยุดเพิ่มฟีเจอร์**
ถ้าไม่ทัน ตัดจาก M6′ ก่อน แล้ว M4 · **ห้ามตัด M3**

> **ถ้า M6′ ไม่ทัน** → ถอยไปใช้ฟอร์มจาก M2 ซึ่งยังอยู่ครบ
> **ห้ามเอาแชตที่ทำงานครึ่ง ๆ ขึ้นเวที** — ฟอร์มที่ใช้ได้จริงดีกว่าแชตที่พังกลางเดโม

### โครงเดโม 3 นาที

```
0:00  "วันนี้ใช้คน 5 คน เต็มเวลา 2 เดือน เพื่อตอบแบบประเมินหนึ่งรอบ
       ปัญหาไม่ใช่ว่าไม่ได้ทำงาน แต่กว่าจะรู้ว่าหลักฐานพอหรือยัง ก็ปลายรอบแล้ว"
0:20  เปิดจากฝั่งกอง — แชต → agent ขึ้นการ์ด "รอบนี้ต้องส่งอะไรบ้าง"
0:40  ⭐ ลากไฟล์ "(ร่าง) แผนดำเนินการ" ของ A-05 เข้าแชต
       → agent ตอบทันที: ชั้น C · "แผนไม่ใช่หลักฐานผลลัพธ์" · บอกว่าต้องการอะไรเพิ่ม (AC-02, AC-06)
1:10  ⭐ พิมพ์ "อัปเดต A-05 เป็น 60%" → **การ์ดยืนยันขึ้น ยังไม่เปลี่ยนอะไร** → กดยืนยัน
       → ตัวเลขซ้ายขยับ แต่ **ตัวเลขขวายังเป็น 0%**                          (AC-17, AC-18, AC-01)
1:40  สลับเป็นทีมกลาง → คิวรอยืนยันชั้น → กดยืนยันของ A-03 → ตัวเลขขวาขยับ
2:10  สร้างเอกสารรายเดือน → เปิดไฟล์ Word → เลื่อนลงท้าย:
       ทะเบียนหมายเหตุ "A-02 ไม่มีหลักฐานรองรับ"                                (AC-15)
2:35  ปิด endpoint สด ๆ → แชตขึ้นสถานะตามจริง · ฟอร์มสำรองยังกรอกได้             (AC-12)
2:45  รัน verify.py → ผ่านครบ
2:55  ปิด: "ระบบนี้ไม่เคยบอกว่าเราผ่านการประเมิน — มันบอกว่าหลักฐานพร้อมให้ตรวจหรือยัง
       และตัวเลขทุกตัวมาจากโค้ด ไม่ได้มาจากโมเดล"
```

---

## 11. Assumptions, decisions and blockers

### 11.1 ข้อสมมติที่รับไว้แล้ว `[ยืนยันแล้ว]`

1. ข้อมูลทั้งหมดเป็น synthetic ที่สร้างขึ้นเอง + ติดป้ายในแอปทุกหน้า
2. ไม่ส่งอีเมล/LINE จริง — ร่างไว้ คนกดส่ง
3. ไม่มี login จริง — ปุ่มสลับบทบาท **แต่บังคับสิทธิ์จริงใน backend** และเขียนกำกับบนจอว่าไม่ใช่ระบบยืนยันตัวตน
4. **โค้ดคำนวณและตัดสินสถานะ โมเดลไม่ทำ**
5. ไม่มีข้อมูล → ตอบว่าไม่มี · ห้ามเดา ห้ามแต่ง
6. คนต้องยืนยันก่อนผลของ AI จะมีผลจริง
7. SQLite บนเครื่อง local · key ฝั่ง backend · ENV · รันในเครือข่ายเดียวกับ endpoint
8. **ห้ามอ้างว่าต่อโมเดลสำเร็จถ้ายังไม่ได้ทดสอบ** — ถ้าใช้ mock ต้องประกาศบนจอ
9. ไม่คำนวณ CO₂ · ไม่ทำ OCR · ไม่นำเข้า Excel อัตโนมัติ · ไม่ทำตัวชี้วัด kWh/จำนวนรถ
10. ผู้ใช้ไม่เกิน ~20 คน · เดโมบนเครื่อง local

### 11.2 การตัดสินใจของผู้ช่วย — ต้องการการยืนยัน `[เสนอ]`

| # | เรื่อง | เหตุผล |
|:---:|---|---|
| D1 | `tracked_item` ตารางเดียวรองรับทั้งข้อประเมินและโครงการ | dashboard · alert · agent ใช้โค้ดชุดเดียว ไม่ต้องเขียนสองระบบ |
| D2 | **สองตัวเลขแยกกัน** งานคืบหน้า / ยืนยันด้วยหลักฐาน ไม่รวมเป็นตัวเดียว | เป็นแกนของ AC-01 และเป็นจุดที่ต่างจาก dashboard ทั่วไป |
| ~~D3~~ | ~~ทางสำรองของ agent เป็นลูปเลือก action ด้วย JSON~~ | **ไม่ได้ใช้** — TF6 ผ่าน ใช้ native tool calling ได้ (§8) · เก็บทางสำรองไว้ใน §5.4 เผื่อ endpoint เปลี่ยน |
| D4 | บันทึกทุกลูปของ agent ลง `agent_run` | เป็นหลักฐานว่า agent ทำงานจริง ไม่ใช่ prompt ที่พูดถึง tool (AC-06) |
| D5 | Next.js + TypeScript + SQLite | ทำครบเส้นได้ในเวลาที่เหลือ · key อยู่ฝั่ง server |
| D6 | เพดาน agent 10 call / 60 วินาที เก็บใน `app_setting` | ปรับได้หน้างานถ้า latency ไม่เป็นใจ · และทดสอบได้ด้วย AC-07 |

### 11.2.1 เปลี่ยนขอบเขต Required เมื่อ 15 ก.ย. `[ยืนยันแล้วโดยทีม]` ⚠️

**ต้องแจ้ง mentor พร้อมตอนส่งแผน — ไม่ใช่เปลี่ยนเงียบ ๆ หลังรีวิว**

| # | เปลี่ยนอะไร | เหตุผล |
|:---:|---|---|
| **D7** | **หน้าเจ้าของข้อมูลเปลี่ยนจากฟอร์ม เป็นแชตที่มี agent** (R3 · §5.4.1 · M6′) | เดิม "ยังขาดอะไร" ที่ agent ผลิตไปค้างในคิวทีมกลาง ไม่ถึงคนที่แก้ได้ · แชตย่นลูปป้อนกลับจากหลายวันเหลือไม่กี่วินาที และทำให้ส่วน agentic มีผู้ใช้จริง |
| **D8** | **agent ในแชตขึ้นการ์ดยืนยัน ไม่เขียนค่าเอง** | รักษา §5.5 ที่ระบุว่า AI กรอกความคืบหน้าไม่ได้ · และกันการตีความภาษาไทยผิด เพราะผู้ใช้เห็นค่าก่อนมันมีผล (AC-17, AC-18) |
| **D9** | **ตัด "กองที่ยังไม่ส่ง + Outbox" จาก Required ไป Optional O1** | เอาเวลามาทำ D7 · คุณค่าบางส่วนได้คืนผ่านการ์ด "สิ่งที่ต้องส่งรอบนี้" ในแชต |
| **D10** | **ฟอร์มกรอกมือจาก M2 ยังอยู่** ไม่ถูกแทนที่ | เป็นทางสำรองบังคับเมื่อโมเดลล่ม (AC-12) · และเป็นทางถอยถ้า M6′ ไม่ทันเที่ยง 16 ก.ย. |

### 11.3 คำถามค้าง `[คำถามค้าง]`

| # | คำถาม | ค่าที่ใช้ไปก่อน |
|:---:|---|---|
| Q1 | ชุดสี / โลโก้มาตรฐานขององค์กร | ธีมเรียบ เขียว-เทา ตาม §4.4 |
| Q2 | เกณฑ์ At Risk ที่ 30 จุด เหมาะกับงานจริงไหม | ใช้ 30 · เก็บใน `app_setting` แก้ได้ |
| Q3 | รอบเอกสารเป็นรายเดือนตายตัว หรือปรับได้ | รายเดือน · เก็บ `period` ในตาราง `response` เผื่อขยาย |

### 11.4 Blockers

| # | Blocker | ผลกระทบ | ต้องทำอะไร |
|:---:|---|---|---|
| ~~**B1**~~ | ~~ยังไม่มีการทดสอบ endpoint~~ | — | ✅ **ปลดแล้ว 15 ก.ย. 2569** — DGX 10.0.63.215 · ไม่ต้อง auth · TF1–TF8 ผ่านครบ · ใช้ทางหลัก §5.4 |
| **B2** | ขอบเขตอาจใหญ่เกินเวลาที่เหลือ | ไม่มีเส้นทางไหนเสร็จสมบูรณ์ | ยึดลำดับตัดใน §10 · **ห้ามตัด M3** · freeze เที่ยง 16 ก.ย. |

---

## 12. Mentor review record

| ช่อง | เนื้อหา |
|---|---|
| Revision ที่ทบทวน | — |
| วันที่ | — |
| ผู้ทบทวน | — |
| ความเห็น | — |
| ผลการทบทวน | ⬜ อนุมัติ ⬜ อนุมัติแบบมีเงื่อนไข ⬜ ให้แก้ |
| เงื่อนไขก่อนลงมือ | — |

> ⏸ **หยุดก่อนลงมือเขียนโค้ดแอปพลิเคชัน** — ส่งแผนนี้ให้ mentor แล้วนำ feedback กลับมา
> (B1 ปลดแล้ว จึง**ไม่ต้องขอให้ mentor รับรองทางสำรอง §5.4 อีก** — ใช้ทางหลักได้เลย)
> **การที่ผู้ช่วยเขียนแผนนี้ไม่นับเป็นการอนุมัติ** และเงื่อนไขก่อนลงมือต้องแก้ให้จบก่อนเริ่ม M1
