# FRONTEND.md — ข้อกำหนดหน้าจอ · GreenATM Evidence & Readiness Platform

| | |
|---|---|
| **อ้างอิง** | `PLAN.md` (ฉบับล่าสุดในโฟลเดอร์นี้) — §3 Scope · §4.3 หน้าจอ · §5.5 ตารางอำนาจ · §6.3 Entities |
| **Revision** | 0.3 · 15 ก.ย. 2026 |
| **สถานะ** | 🔷 `[เสนอ]` — รายการหน้าผ่านการตกลงแล้ว · รายละเอียดใน wireframe ยังแก้ได้ |
| **Stack** | Next.js App Router + TypeScript · Tailwind + shadcn/ui (ตาม §7) |

> เอกสารนี้ไม่ตั้งข้อกำหนดใหม่ — แปลงข้อกำหนดใน `PLAN.md` ให้เป็นสิ่งที่ลงมือสร้างได้
> ทุกจุดที่เพิ่มเกินแผน ติดป้าย `[เพิ่มจาก FRONTEND]` ไว้
>
> **แก้ใน 0.2** — ① wireframe ของ A-02 เดิมขัดกับ AC-01 (วาดเป็น 80% + มีเอกสารชั้น A)
> แก้ให้ตรงกับ `sample-data/manifest.json` แล้ว · ② เพิ่มหน้าจอถามวันที่เอกสาร (AC-03)
> ③ เพิ่มสเปกกล่องอัปโหลดหลักฐาน · ④ เพิ่ม breakpoint สำหรับหน้าผู้บริหารบนมือถือ
>
> ⚠️ **ตัวเลขทุกตัวใน wireframe ต้องตรงกับ `sample-data/manifest.json`** — ถ้าแก้ข้อมูล ต้องแก้ที่นี่ด้วย

---

## 1. โครง route

7 เส้นทางจริง + 2 optional · หน้าที่แผนระบุว่าต้องมีแต่ยังไม่มี route (แก้ร่างคำตอบ · คิวอนุมัติ) **ยุบเป็นแท็บ/drawer ในหน้าเดิม** เพื่อประหยัดเวลา

```
app/
  layout.tsx                    ← Shell: header · role switch · nav ตามบทบาท · แถบ Mockup
  page.tsx                      /            หน้าแรก (Dashboard)
  my/page.tsx                   /my          แชตกับผู้ช่วย ⭐ หน้าหลักของเจ้าของข้อมูล (§5.2)
  my/form/page.tsx              /my/form     ฟอร์มกรอกมือ — ทางสำรองเมื่อโมเดลล่ม (AC-12)
  items/page.tsx                /items       รายการทั้งหมด
  items/[id]/page.tsx           /items/:id   รายละเอียดรายการ  ⭐ หน้าที่ทำงานหนักที่สุด
  review/page.tsx               /review      ศูนย์ตรวจสอบ (แท็บ A–E)
  documents/page.tsx            /documents   เอกสารรายเดือน
  settings/page.tsx             /settings    ตั้งค่าเกณฑ์ · น้ำหนัก · ค่าคงที่
  audit/page.tsx                /audit       [P2] ประวัติการแก้ไข
  ask/page.tsx                  /ask         [P2] แชตบอต (O1)
```

**ที่ยุบเข้าไป**

| สิ่งที่แผนต้องการ | อยู่ที่ไหน |
|---|---|
| แก้ร่างคำตอบ (R8/R9) | Drawer ในแท็บ B ของ `/review` |
| คิวอนุมัติของผู้บริหาร (§5.5) | แท็บ E ของ `/review` — โผล่เฉพาะบทบาทผู้บริหาร |
| แผง "agent ทำอะไรไปบ้าง" | Drawer ใน `/items/:id` `[เพิ่มจาก FRONTEND]` |
| กรอกความคืบหน้า / แนบหลักฐาน (R3/R4) | **การ์ดในแชต `/my`** — ฟอร์มเดิมย้ายไป `/my/form` เป็นทางสำรอง |

---

## 2. Shell ที่ใช้ร่วมทุกหน้า

```text
┌────────────────────────────────────────────────────────────────────────────┐
│ ⬤ GreenATM Readiness    [บทบาท: ทีมกลาง ▾]   🔔 Outbox 3    รอบ ก.ย. 2569 │
├────────────────────────────────────────────────────────────────────────────┤
│ ⚠ ข้อมูลตัวอย่าง (Mockup) — ไม่ใช่สถานะจริงขององค์กร        [ซ่อนไม่ได้]  │
├──────────────┬─────────────────────────────────────────────────────────────┤
│ หน้าแรก       │                                                             │
│ งานของฉัน     │                    เนื้อหาของแต่ละหน้า                       │
│ รายการทั้งหมด │                                                             │
│ ศูนย์ตรวจสอบ  │                                                             │
│ เอกสารรายเดือน│                                                             │
│ ตั้งค่า       │                                                             │
└──────────────┴─────────────────────────────────────────────────────────────┘
```

### เมนูเปลี่ยนตามบทบาท — และนั่นคือหลักฐานที่มองเห็นได้

| เมนู | เจ้าของข้อมูล | ทีมกลาง | ผู้บริหาร |
|---|:---:|:---:|:---:|
| หน้าแรก | ✅ | ✅ | ✅ |
| **แชตกับผู้ช่วย** | ✅ **หน้าหลัก** | ✅ | — |
| รายการทั้งหมด | อ่านอย่างเดียว | ✅ | อ่านอย่างเดียว |
| ศูนย์ตรวจสอบ | — | ✅ (A–D) | ✅ (แท็บ E เท่านั้น) |
| เอกสารรายเดือน | — | ✅ | ✅ |
| ตั้งค่า | — | ✅ | — |

> **backend ต้องบังคับซ้ำอีกชั้นเสมอ** — เมนูที่หายไปคือความสะดวก ไม่ใช่การรักษาสิทธิ์ (R2)
> ตอนเดโม การสลับบทบาทแล้ว**เมนูหายไปต่อหน้ากรรมการ** คือการพิสูจน์ที่ไม่ต้องอธิบาย

---

## 3. Design tokens

### 3.1 พื้นและตัวอักษร

| บทบาท | Light | Dark |
|---|---|---|
| พื้นหน้า (page) | `#f9f9f7` | `#0d0d0d` |
| พื้นการ์ด/ตาราง | `#fcfcfb` | `#1a1a19` |
| ตัวอักษรหลัก | `#0b0b0b` | `#ffffff` |
| ตัวอักษรรอง | `#52514e` | `#c3c2b7` |
| ตัวอักษรจาง (แกน/ป้าย) | `#898781` | `#898781` |
| เส้นตาราง | `#e1e0d9` | `#2c2c2a` |
| ขอบ hairline | `rgba(11,11,11,.10)` | `rgba(255,255,255,.10)` |
| accent (ตัวเลขที่นับจริง) | `#2a78d6` | `#3987e5` |

- ฟอนต์: `system-ui, -apple-system, "Segoe UI", sans-serif` ทั้งระบบ ไม่มี display/serif
- **`tabular-nums` เฉพาะตัวเลขในคอลัมน์ตาราง** · ตัวเลขใหญ่ใน KPI ใช้ figure ปกติ
- ตัวอักษรในตารางไม่เล็กกว่า 14px · contrast ≥ 4.5:1 (§4.4)

### 3.2 ชั้นหลักฐาน A–D — ordinal ramp สีเดียว (ผ่าน validator แล้ว)

| ชั้น | สัญลักษณ์ | Light | Dark | ความหมาย |
|---|:---:|---|---|---|
| A | ◆ | `#184f95` | `#184f95` | ทวนสอบจากภายนอก / วัดได้พร้อมที่มา |
| B | ◇ | `#2a78d6` | `#3987e5` | เอกสารภายในที่อนุมัติแล้ว |
| C | ○ | `#86b6ef` | `#9ec5f4` | ร่าง · แผน · ความตั้งใจ — **ไม่ดันตัวเลข** |
| D | ✕ | `#898781` | `#898781` | ไม่มีเอกสาร — เข้าทะเบียนหมายเหตุ |

```
validate_palette.js  light  #184f95,#2a78d6,#86b6ef  --ordinal  → ALL PASS
validate_palette.js  dark   #184f95,#3987e5,#9ec5f4  --ordinal  → ALL PASS
```

> **สัญลักษณ์มาก่อนสี** — badge ต้องเป็น `◆ A` เสมอ ห้ามเป็นจุดสีเปล่า ๆ (§4.4)

### 3.3 สถานะ — จานสีสงวน ห้ามเอาไปใช้เป็นสีข้อมูล

| สถานะ | สี | ไอคอน + ป้าย |
|---|---|---|
| On Track | `#0ca30c` | ✓ ตามแผน |
| At Risk | `#fab219` | ⚠ เสี่ยง |
| Delayed | `#d03b3b` | ⛔ ล่าช้า |
| รอตรวจ / ถูกตีกลับ | `#ec835a` | ↩ ตีกลับ |

สีสถานะ **ต้องมาคู่กับไอคอนและข้อความเสมอ** — ห้ามสื่อความหมายด้วยสีอย่างเดียว

### 3.4 Breakpoint `[เพิ่มจาก FRONTEND]`

`PLAN.md` §4.4 ระบุว่า **หน้าผู้บริหารต้องอ่านบนมือถือได้** ส่วนหน้าอื่นเป็น laptop-first

| ช่วงกว้าง | หน้าแรก (`/`) | หน้าอื่น |
|---|---|---|
| **≥ 1280px** (หลัก) | เต็มรูปแบบ · KPI คู่เรียงแนวนอน · ตารางทุกคอลัมน์ | เต็มรูปแบบ |
| **1024–1279px** | ตารางซ่อนคอลัมน์ "อัปเดตล่าสุด" · sidebar ยุบเป็นไอคอน | ใช้ได้ · `/items/:id` สามคอลัมน์บีบเหลือ 2 + 1 ด้านล่าง |
| **< 768px** (มือถือ) | **"สิ่งที่ต้องตัดสิน" + KPI คู่ + 5 รายการแรกเท่านั้น** · KPI คู่เรียงแนวตั้งแต่**ยังอยู่ในจอเดียวกัน** · ตารางกลายเป็นการ์ดต่อรายการ | ไม่รองรับ — ขึ้นข้อความว่าให้เปิดบน laptop |

**กฎที่ห้ามหลุดบนมือถือ:** KPI คู่ต้องยังเห็นพร้อมกันในจอเดียว **ห้ามให้ต้องเลื่อนเพื่อเห็นตัวที่สอง**
ไม่งั้น V1 พังบนอุปกรณ์ที่ผู้บริหารใช้จริง

---

## 4. กฎภาพ 5 ข้อที่บังคับกติกาของแผน

การออกแบบภาพคือการบังคับวิธีการ ไม่ใช่การตกแต่ง

| # | กฎ | บังคับเพราะ |
|---|---|---|
| **V1** | **สองตัวเลขแสดงคู่กันเสมอ ขนาดเท่ากัน ห้ามรวม ห้ามซ้อนในแท่งเดียว** | §4.2 · AC-01 |
| **V2** | **ไม่มี hero number เดียวบนหน้าแรก** — เพราะตัวเลขเดียวที่สรุปทุกอย่างคือตัวเลขที่ระบบปฏิเสธจะผลิต | §4.2 |
| **V3** | **"สิ่งที่ต้องตัดสิน" อยู่เหนือ KPI เสมอ** ไม่ว่าจะมีกี่รายการ · ถ้าไม่มีให้ขึ้นว่า "ไม่มีรายการค้างตัดสิน" ไม่ใช่ซ่อนกล่อง | §4.3 |
| **V4** | **ทุกตัวเลขที่แสดงต้องคลิกกลับไปถึงที่มาได้** — ไม่มีตัวเลขที่พิมพ์ลง JSX ตรง ๆ | §4.4 · §5.6 |
| **V5** | **ข้อความที่ AI เรียบเรียง ต้องมีป้ายกำกับเสมอ** `[สังเคราะห์จาก E-003, E-011]` | §5.6 · AC-10 |

**ห้าม:** hero banner · gradient ฟุ้ง · กราฟสองแกน · แท่งเดียวที่รวมงานคืบหน้ากับหลักฐาน · chat ที่ไม่มีหน้าที่ในเวิร์กโฟลว์

---

## 5. ราย route

### 5.1 `/` หน้าแรก — Dashboard

**ใคร:** ทุกบทบาท (เนื้อหาเหมือนกัน สิทธิ์ปุ่มต่างกัน) · **อ่านบนมือถือได้** (§4.4)

```text
┌─ 📋 สิ่งที่ต้องตัดสิน ──────────────────────────────── 3 รายการ ─┐
│ ⬜  A-02  งานเสร็จ 100% · หลักฐานที่นับได้ 0 ชิ้น      [เปิดดู] │
│ 🛑  A-04  หลักฐานขัดกับ A-08                          [เปิดดู] │
│ ⚠   กอง C ยังไม่ส่งข้อมูลรอบนี้ · เลยกำหนด 6 วัน   [สร้างร่าง] │
└──────────────────────────────────────────────────────────────┘

┌── งานคืบหน้า ────┐  ┌── ยืนยันด้วยหลักฐาน ──┐  ┌ รอตรวจ ┐ ┌ ยังไม่ส่ง ┐
│      68%         │  │        41%            │  │   5    │ │    3     │
│ เจ้าของกรอกเอง    │  │ ทีมกลางยืนยันแล้ว      │  └────────┘ └──────────┘
│ ▓▓▓▓▓▓▓░░░ สีเทา │  │ ▓▓▓▓░░░░░░ สี accent   │
└──────────────────┘  └───────────────────────┘
   ▲ ตัวเลขนี้ไม่ดันตัวเลขขวา — เขียนกำกับไว้ใต้การ์ดตรง ๆ

ตารางรายการทั้งหมด
 รหัส │ ชื่อรายการ │ กอง │ น้ำหนัก │ งานคืบหน้า │ ยืนยันแล้ว │ สถานะ │ อัปเดตล่าสุด
 A-02 │ ระบบจัดการสิ่งแวดล้อม… │ ปฏิบัติการ │      8  │      100%  │        0% │ ⛔     │ 12 ก.ย.

[สร้างเอกสารรายเดือน]   ← ผู้บริหารเท่านั้นที่กดได้ (§5.5) · บทบาทอื่นเห็นปุ่มจาง + tooltip
```

**สีของ KPI คู่:** งานคืบหน้า = ตัวอักษรรอง `#52514e` · ยืนยันแล้ว = accent `#2a78d6`
→ ตัวเลขที่ "นับจริง" คือตัวที่มีสี · เป็นการเข้ารหัสกติกา §4.2 ลงในภาพ

**API:** `GET /api/dashboard` → `{decisions[], kpi{progress,verified,pending,missing}, items[]}`

---

### 5.2 `/my` แชตกับผู้ช่วย ⭐ `[แก้ 0.3]`

**ใคร:** เจ้าของข้อมูล (ทีมกลางเปิดดูได้) · **หน้าหลักของบทบาทนี้** แทนตารางเดิม
**ทำไม:** เดิม "ยังขาดอะไร" ที่ agent ผลิตไปค้างในคิวทีมกลาง ไม่ถึงคนที่แก้ได้ (PLAN.md §5.4.1)

```text
กองสิ่งแวดล้อม · รอบ ก.ย. 2569                        [ใช้ฟอร์มแทน →]  ← ลิงก์เล็ก ไม่เด่น

┌─ ผู้ช่วย ─────────────────────────────────────────────────────┐
│ รอบนี้กองคุณมี 2 รายการที่ยังไม่ส่ง                              │
│ ┌ สิ่งที่ต้องส่งรอบนี้ ─────────────────────────────────┐        │
│ │ A-05  ลดการใช้พลังงาน       กำหนด 5 ก.ย. · เลย 10 วัน │        │
│ │ A-12  จัดการของเสียอันตราย   กำหนด 5 ก.ย. · เลย 10 วัน │        │
│ │ ↩ A-04 ถูกตีกลับ — "ข้อมูลเชิงปริมาณยังไม่แนบมา"      │        │
│ └────────────────────────────────────────────────────┘        │
└───────────────────────────────────────────────────────────────┘

                    ┌─ คุณ ───────────────────────────────────┐
                    │ 📎 (ร่าง) แผนดำเนินการ 2570.docx  → A-05 │
                    └─────────────────────────────────────────┘

┌─ ผู้ช่วย ─────────────────────────────────────────────────────┐
│ ┌ ผลประเมินหลักฐาน · E-010 ──────────────────────────┐        │
│ │ ○ C  ร่าง/แผน — ไม่นับเข้าตัวเลข                      │        │
│ │ เหตุผล: เป็นแผนที่ตั้งใจจะทำ ยังไม่มีผลการวัดใด ๆ       │        │
│ │ ยังขาด: ☐ ผลการวัดการใช้พลังงานจริงเทียบปีฐาน        │        │
│ │        ☐ เอกสารที่ผ่านการอนุมัติ ไม่ใช่ร่าง            │        │
│ │                                    [แนบเพิ่ม]        │        │
│ └────────────────────────────────────────────────────┘        │
└───────────────────────────────────────────────────────────────┘

                              ┌─ คุณ ─────────────────────────┐
                              │ อัปเดต A-05 เป็น 60%           │
                              └───────────────────────────────┘

┌─ ผู้ช่วย ─────────────────────────────────────────────────────┐
│ ┌ ยืนยันความคืบหน้า ────────────────────────────────┐          │
│ │ A-05 · M1 จัดทำแผนดำเนินการ                        │          │
│ │ ความคืบหน้า   40%  →  60%                          │          │
│ │ วันที่ที่บันทึก  15 ก.ย. 2569                        │          │
│ │ ⚠ ยังไม่มีผลจนกว่าจะกดยืนยัน                        │          │
│ │                          [ยืนยัน]  [แก้ค่า]         │          │
│ └──────────────────────────────────────────────────┘          │
└───────────────────────────────────────────────────────────────┘

[พิมพ์ข้อความ…]                                        [📎 แนบไฟล์]
```

**การ์ด 4 แบบ**

| การ์ด | เกิดเมื่อ | หมายเหตุสำคัญ |
|---|---|---|
| **สิ่งที่ต้องส่งรอบนี้** | เปิดแชตครั้งแรก | รวมรายการที่ถูกตีกลับพร้อมเหตุผล · ได้คุณค่าบางส่วนของ M6 ที่ตัดไปคืนมา |
| **ยืนยันความคืบหน้า** | agent ตีความตัวเลขได้ | **แสดงค่าเดิม → ค่าใหม่ ชัด ๆ ก่อนกด** · ยังไม่กด = ยังไม่เปลี่ยน (AC-17) |
| **ผลประเมินหลักฐาน** | แนบไฟล์แล้ว agent จัดชั้นเสร็จ | ชั้น + เหตุผล + "ยังขาดอะไร" — คือของเดิมจาก §5.4 แค่ส่งมาถึงคนที่แก้ได้ |
| **กรอกวันที่เอกสาร** | สกัด `document_date` ไม่ได้ | ใช้เนื้อหาเดียวกับ §5.4.2 |

**กฎที่ห้ามพลาด**

- **ตีความไม่ชัด → ถามกลับ ห้ามขึ้นการ์ด** เช่น "เกือบเสร็จแล้ว" ต้องถามว่ากี่เปอร์เซ็นต์ (AC-19)
- **อ้างถึงรายการของกองอื่น → ปฏิเสธในแชต** และ backend ต้องตอบ 403 ถ้ายิงตรง (AC-20)
- **ลิงก์ "ใช้ฟอร์มแทน" ต้องอยู่เสมอ** และเด่นขึ้นอัตโนมัติเมื่อโมเดลล่ม (AC-12)

**สถานะว่าง:** "ไม่มีงานค้างรอบนี้" + วันที่รอบถัดไป — **ไม่ใช่หน้าเปล่า** (§4.1 ข้อ 1)

**API:** `GET /api/my-work` · `POST /api/chat` · `POST /api/items/:id/progress` *(เรียกตอนกดยืนยันการ์ด — actor = ผู้ใช้)* · `POST /api/items/:id/evidence` · `POST /api/evidence/:id/document-date`

---

### 5.3 `/items` รายการทั้งหมด

ตารางเดียวกับหน้าแรกแถว 3 แต่กรองได้: กอง · สถานะ · ชั้นหลักฐานต่ำสุด · ยังไม่ส่ง
ตัวกรองอยู่แถวเดียวเหนือตาราง · เรียงได้ทุกคอลัมน์ · ตัวเลขใช้ `tabular-nums`

**API:** `GET /api/items?division=&status=&tier=`

---

### 5.4 `/items/:id` รายละเอียดรายการ ⭐

หน้าที่ต้องเนี้ยบที่สุด — เดโมนาทีที่ 0:25–1:20 อยู่ที่นี่ทั้งหมด

> **A-02 คือเคส AC-01** — งานเสร็จ **100%** แต่หลักฐานเป็นชั้น C ทั้งคู่ **ไม่มี A/B เลย**
> ตัวเลขขวาจึงต้องเป็น **0%** · ถ้าภาพนี้วาดผิด เดโมนาทีที่ 0:45 จะพัง
> ค่าทุกตัวด้านล่างมาจาก `sample-data/manifest.json` ห้ามแต่งขึ้นเอง

```text
A-02 · ระบบจัดการสิ่งแวดล้อมและรอบการตรวจติดตามภายใน
กองปฏิบัติการจราจรทางอากาศ · น้ำหนัก 8 · ⛔ ล่าช้า (เลยกำหนด 10 วัน)
─────────────────────────────────────────────────────────────────────────────
[ ความคืบหน้า ]         [ หลักฐาน ]                    [ ยังขาดอะไร ]
 milestone 10/10         E-003 ○ C  บันทึกประชุม 6/2569   ☐ รายงานผลการตรวจ
 ▓▓▓▓▓▓▓▓▓▓ 100%           ยังไม่ผ่านการรับรอง               ติดตามภายในรอบแรก
                           [ยืนยันชั้น] [แก้ชั้น]              ที่ลงนามแล้ว
 M1  ✓ 100%  20 ส.ค.     ⚠ ร่าง/บันทึก — ไม่นับเข้าตัวเลข
 M2  ✓ 100%  20 ส.ค.                                      ☐ ทะเบียนข้อบกพร่อง
 M3  ✓ 100%  20 ส.ค.     E-004 ○ C  สไลด์ความคืบหน้า          ที่ปิดแล้ว
 …   (ครบทั้ง 10)          ไม่มีเอกสารอ้างอิงประกอบ
 M10 ✓ 100%  20 ส.ค.      ⚠ ร่าง/บันทึก — ไม่นับเข้าตัวเลข      เสนอโดย agent
 [แก้ความคืบหน้า]         [+ แนบหลักฐาน]                    13 ก.ย. 14:22
─────────────────────────────────────────────────────────────────────────────
 งานคืบหน้า 100%        ยืนยันด้วยหลักฐาน 0%     ← คู่กัน ขนาดเท่ากัน (V1)
 ▲ เจ้าของรายงานเอง      ▲ ไม่มีหลักฐานชั้น A/B เลย จึงเป็น 0
 [▸ agent ทำอะไรไปบ้าง]  [▸ ประวัติการแก้ / การตีกลับ]
```

**Drawer "agent ทำอะไรไปบ้าง"** `[เพิ่มจาก FRONTEND]` — อ่านจาก `agent_run.tool_calls_json`

```text
รอบประเมิน 13 ก.ย. 14:22 · ใช้ 6 จาก 10 call · 23 วินาที · ผลลัพธ์: เสร็จสมบูรณ์
 1  get_item(A-02)                          →  เกณฑ์ 1 ข้อ น้ำหนัก 8
 2  list_evidence(A-02)                     →  2 ชิ้น
 3  read_evidence(E-003)  1,400 อักษร       →  บันทึกประชุม ยังไม่ผ่านการรับรอง
 4  propose_tier(E-003, C, "บันทึกการประชุมที่ยังไม่รับรอง ไม่มีผลการตรวจติดตาม")
 5  read_evidence(E-004)    900 อักษร       →  สไลด์นำเสนอ ไม่มีเอกสารอ้างอิง
 6  propose_tier(E-004, C, "สไลด์นำเสนอภายใน ไม่ใช่หลักฐานผลลัพธ์")
 ⟶ propose_gap(A-02, ["รายงานผลการตรวจติดตามภายในรอบแรกที่ลงนามแล้ว",
                       "ทะเบียนข้อบกพร่องที่ปิดแล้ว"])
```

> นี่คือหน้าจอที่ตอบคำถาม **"รู้ได้ไงว่าไม่ใช่แค่เรียก LLM ครั้งเดียว"** โดยไม่ต้องพูดอะไร

**สิทธิ์:** ปุ่ม [ยืนยัน]/[แก้ชั้น] เห็นเฉพาะทีมกลาง · เจ้าของข้อมูลเห็นแต่กรอก/แนบของกองตัวเอง

**สถานะที่ต้องออกแบบ:** กำลังประเมิน (พร้อมปุ่มยกเลิก) · ประเมินไม่ครบเพราะชนเพดาน · **ประเมินไม่สำเร็จ (JSON เสีย 2 ครั้ง) → ให้ทีมกลางจัดชั้นเอง** · โมเดลปิดอยู่ → ปุ่ม "ให้ agent ประเมิน" จาง + ข้อความตามจริง

**API:** `GET /api/items/:id` · `POST /api/items/:id/agent-run` · `POST /api/evidence/:id/confirm-tier` · `GET /api/items/:id/agent-runs`

#### 5.4.1 กล่องแนบหลักฐาน `[เพิ่มจาก FRONTEND]`

```text
┌ แนบหลักฐาน · A-02 ───────────────────────────────────────────┐
│  ลากไฟล์มาวาง หรือ [เลือกไฟล์]                                │
│  รองรับ PDF · DOCX · XLSX · PNG · JPG · ไม่เกิน 20 MB/ไฟล์     │
│  ⚠ ไม่ทำ OCR — ไฟล์ภาพที่ไม่มีข้อความจะอ่านเนื้อหาไม่ได้        │
│                                                               │
│  ชนิดไฟล์ไม่รองรับ → "รองรับเฉพาะ PDF, DOCX, XLSX, PNG, JPG"  │
│  ขนาดเกิน        → "ไฟล์ 32 MB เกินขีดจำกัด 20 MB"            │
│                     ทั้งสองกรณี: ไม่อัปโหลด ไม่บันทึกอะไรเลย    │
└───────────────────────────────────────────────────────────────┘
```

#### 5.4.2 เอกสารไม่มีวันที่ — ระบบต้องถาม (AC-03) ⭐ `[เพิ่มจาก FRONTEND]`

หลังอัปโหลด ถ้าสกัด `document_date` จากเนื้อเอกสารไม่ได้ **ต้องถามก่อน ห้ามข้าม
และห้าม fallback ไปใช้วันที่อัปโหลด** — เอกสารจะยังไม่เข้าคิวประเมินจนกว่าจะตอบ

```text
┌ ต้องการข้อมูลเพิ่ม · E-021 ──────────────────────────────────┐
│ ⚠ ไม่พบวันที่ในตัวเอกสารนี้                                   │
│                                                              │
│ ระบบไม่ใช้วันที่อัปโหลด (13 ก.ย. 2569) แทน เพราะอาจคลาดเคลื่อน │
│ จากวันที่จริงของเอกสาร และมีผลต่อการตัดสินว่าหลักฐานล้าสมัยไหม  │
│                                                              │
│ วันที่ในเอกสาร  [ __ / __ / ____ ]                            │
│ ○ เอกสารนี้ไม่มีวันที่จริง ๆ → บันทึกเป็น "ไม่ระบุวันที่"        │
│                              และนับเป็นข้อสังเกตในทะเบียนหมายเหตุ │
│                                     [ยกเลิก]  [บันทึก]        │
└──────────────────────────────────────────────────────────────┘
```

- สถานะระหว่างรอ: เอกสารขึ้นป้าย **"รอข้อมูลวันที่"** ในคอลัมน์หลักฐาน · ปุ่ม "ให้ agent ประเมิน" ปิดอยู่
- ตัวเลือกที่สอง (ไม่มีวันที่จริง ๆ) มีไว้เพื่อไม่ให้ผู้ใช้ติดตาย — แต่**ต้องบันทึกว่าเป็นข้อสังเกต** ไม่ใช่เดาวันที่ให้
- **API:** `POST /api/evidence/:id/document-date`

---

### 5.5 `/review` ศูนย์ตรวจสอบ

```text
[ A รอยืนยันชั้น 7 ] [ B ร่างคำตอบ 4 ] [ C ยังไม่ส่ง 3 ] [ D Outbox 2 ] [ E อนุมัติ 2 ]
                                                                          ▲ ผู้บริหารเท่านั้น
A · รอยืนยันชั้นหลักฐาน
 เอกสาร │ รายการ │ ชั้นที่ agent เสนอ │ เหตุผล │ [ยืนยัน] [แก้เป็น ▾ + เหตุผล]

B · ร่างคำตอบรอตรวจ            → คลิกเปิด Drawer แก้ร่าง
 รายการ │ สถานะ │ ⚠ ข้อมูลไม่พอ │ อัปเดตล่าสุด

C · ยังไม่ส่ง
 กอง │ รายการ │ เลยกำหนด (วัน) │ ติดต่อ │ [สร้างร่างข้อความ]

D · Outbox
 ถึง │ เรื่อง │ ตัวอย่างข้อความ │ [ดู/แก้] [ส่ง]     ← คนกดเอง ไม่ออกนอกระบบ

E · รออนุมัติ (ผู้บริหาร)
 รายการ │ คำตอบ │ หลักฐานอ้างอิง │ [อนุมัติ] [ตีกลับ + เหตุผล(บังคับ)]
```

**Drawer แก้ร่างคำตอบ**

```text
A-06 · คำตอบรอบ ก.ย. 2569                          ⚠ ระบบทำเครื่องหมาย: ข้อมูลไม่พอ
┌ ข้อความ ─────────────────────────────┐ ┌ ตัวอ้างอิงที่ใช้ ──────────────┐
│ ย่อหน้า 1 …                           │ │ ◆ E-014 น.3   [แทรกตัวอ้างอิง] │
│   [อ้างอิง E-014]                     │ │ ◇ E-009 น.1                   │
│ ย่อหน้า 2 …                           │ │ ○ E-021 — ไม่นับ               │
│   [สังเคราะห์จาก E-009, E-014]        │ └───────────────────────────────┘
└──────────────────────────────────────┘
⛔ ย่อหน้าที่ยังไม่มีตัวอ้างอิง: 1        ← ส่งขออนุมัติไม่ได้จนกว่าจะเป็น 0 (AC-10)
                                          [บันทึกร่าง] [ส่งขออนุมัติ]
```

**API:** `GET /api/review/:tab` · `POST /api/responses/:id/claims` · `POST /api/responses/:id/submit` · `POST /api/responses/:id/approve|reject` · `POST /api/outbox/:id/send`

---

### 5.6 `/documents` เอกสารรายเดือน

```text
รอบ ก.ย. 2569                                    [Export Word] [Export PDF]
 รายการ │ คำตอบ │ สถานะ │ ตัวอ้างอิง │ ผู้อนุมัติ
 A-01   │ …     │ ✓ อนุมัติแล้ว │ 3 │ ผู้บริหาร · 14 ก.ย.
 A-02   │ —     │ ⛔ ไม่มีหลักฐาน │ 0 │ —        ← ไม่หายไปจากตาราง

⚠ ทะเบียนหมายเหตุ (3 รายการ)                              ← ท้ายหน้า ห้ามซ่อน
 รายการ │ เจ้าของ │ สาเหตุ │ อัปเดตล่าสุด │ ค้างมากี่รอบ
 A-02   │ ปฏิบัติการ │ หลักฐานชั้น C ทั้งหมด ไม่มี A/B │ 12 ก.ย. │ 2
```

**ถ้า export ไม่ผ่านเพราะมีข้อความไม่มีตัวอ้างอิง:** ขึ้น error ที่ **ชี้ไปถึงย่อหน้าที่ผิด** พร้อมลิงก์ไปแก้ — ไม่ใช่ toast ลอย ๆ (AC-10)

**API:** `GET /api/documents/:period` · `POST /api/documents/:period/export?format=docx|pdf`

---

### 5.7 `/settings` ตั้งค่า

**บังคับโดย R14 + AC-11** — แก้แล้วต้องมีผลทันทีโดยไม่ restart

```text
ค่าคงที่ของระบบ (app_setting)
 คีย์                      │ ค่า │ คำอธิบาย                    │ แก้ล่าสุด
 at_risk_threshold_points  │ 30  │ ต่ำกว่าแผนกี่แต้มถึงนับว่าเสี่ยง │ ทีมกลาง · 14 ก.ย.
 alert_lead_days           │ 15  │ เตือนล่วงหน้ากี่วัน             │
 monthly_due_day           │  5  │ กำหนดส่งของแต่ละรอบ            │
 evidence_stale_months     │ 12  │ หลักฐานเก่ากว่านี้ = ล้าสมัย     │
 agent_max_tool_calls      │ 10  │ เพดาน tool call ต่อรายการ      │
 agent_timeout_seconds     │ 60  │ เพดานเวลาต่อรายการ             │

น้ำหนักรายการ
 รายการ │ น้ำหนัก │ [แก้]        รวม: 100 ✓
 ⚠ แก้ค่าหนึ่ง → ระบบปรับสัดส่วนที่เหลือให้ แล้ว **ต้องกดยืนยันอีกครั้ง**
 ⛔ รวมไม่ถึง 100 → ปุ่มบันทึกปิด
```

**API:** `GET/PUT /api/settings` · `PUT /api/items/weights`

---

## 6. Component inventory

| Component | ใช้ที่ไหน | จุดที่ห้ามพลาด |
|---|---|---|
| `RoleSwitch` | shell | เปลี่ยนเมนู + ยิง header ไป backend ทุก request |
| `MockupBanner` | shell | ไม่มี prop ให้ปิด |
| `DecisionList` | `/` | ว่างแล้วต้องขึ้นข้อความ ไม่ใช่หาย (V3) |
| `KpiPair` | `/` `/items/:id` | รับสองค่าเสมอ — **ไม่มี prop ให้ส่งค่าเดียว** (V1) |
| `TierBadge` | ทุกที่ | `◆ A` สัญลักษณ์ + ตัวอักษร ห้ามสีเปล่า |
| `StatusChip` | ตาราง | ไอคอน + ข้อความ + สี ครบสามอย่าง |
| `ItemTable` | `/` `/items` | `tabular-nums` · เรียง/กรองได้ |
| `MilestoneEditor` | `/items/:id` `/my/form` | validate 0–100 · วันที่ · ชี้ฟิลด์ที่ผิดเป็นภาษาไทย |
| `ChatShell` | `/my` | ประวัติบทสนทนา · แนบไฟล์แบบลากวาง · สถานะ "กำลังตีความ" |
| `TodoCard` | `/my` | รวมรายการที่ถูกตีกลับพร้อมเหตุผล ไม่ใช่แค่รายการค้าง |
| `ConfirmProgressCard` ⭐ | `/my` | แสดง **ค่าเดิม → ค่าใหม่** · **ไม่มี prop ให้ commit เอง** รับแต่ callback ที่ผูกกับปุ่ม (AC-17) |
| `EvidenceResultCard` | `/my` | ชั้น + เหตุผล + "ยังขาดอะไร" · ใช้ `TierBadge` ตัวเดิม |
| `DateAskCard` | `/my` | เนื้อหาเดียวกับ §5.4.2 · **ห้ามมี default เป็นวันที่อัปโหลด** |
| `EvidenceCard` | `/items/:id` | ชั้น + เหตุผล + ปุ่มยืนยัน (ตามสิทธิ์) |
| `GapChecklist` | `/items/:id` | ติ๊กได้ · บอกว่าใครเสนอ |
| `AgentRunDrawer` | `/items/:id` | แสดง call จริงจากฐานข้อมูล ห้าม hardcode |
| `ResponseEditor` | `/review` | นับย่อหน้าที่ไม่มีตัวอ้างอิง · ปิดปุ่มส่งเมื่อ > 0 |
| `CitationChip` | `/review` `/documents` | คลิกเปิดเอกสารต้นทาง (V4) |
| `RemarkRegister` | `/documents` | เรียงตามจำนวนรอบที่ค้าง |
| `EmptyState` `ErrorState` `ModelDownNotice` | ทุกหน้า | ข้อความตามจริง ไม่ใช่ spinner ค้าง |

---

## 7. สถานะที่ต้องออกแบบครบ (§4.4)

| สถานะ | แสดงอย่างไร |
|---|---|
| ยังไม่มีข้อมูล | ข้อความ + สิ่งที่ควรทำต่อ ไม่ใช่กล่องเปล่า |
| กำลังโหลด | skeleton ของตาราง ไม่ใช่ spinner กลางจอ |
| **agent กำลังประเมิน** | แถบความคืบหน้า + จำนวน call ที่ใช้ไป + **ปุ่มยกเลิก** |
| **agent กำลังตีความข้อความ** (`/my`) | ฟองข้อความ "กำลังอ่าน…" + ปุ่มยกเลิก · ไม่ใช่ spinner ค้าง |
| **ตีความไม่ชัด → ถามกลับ** (`/my`) | agent ถามเป็นคำถามตรง ๆ **ไม่ขึ้นการ์ด ไม่เดาค่า** (AC-19) |
| **โมเดลล่มขณะอยู่ในแชต** | ฟองข้อความแจ้งตามจริง + **ปุ่ม "ไปหน้าฟอร์ม" เด่นขึ้น** · ข้อความเก่ายังอ่านได้ (AC-12) |
| ประเมินไม่ครบ (ชนเพดาน) | บอกตรง ๆ ว่าอ่านไปกี่ชิ้นจากทั้งหมด **ไม่เดาให้จบ** |
| **โมเดลล้มเหลว** | "ประเมินไม่สำเร็จ" + ปุ่มให้ทีมกลางจัดชั้นเอง — ส่วนอื่นของระบบยังทำงาน (AC-12) |
| **เอกสารไม่มีวันที่ (AC-03)** | กล่องถามวันที่ตาม §5.4.2 · เอกสารขึ้นป้าย "รอข้อมูลวันที่" · **ห้ามใช้วันที่อัปโหลดแทน** |
| ไฟล์ไม่รองรับ / ขนาดเกิน | ข้อความระบุชนิดหรือขนาดที่รับได้ · ไม่อัปโหลด ไม่บันทึกอะไรเลย (§5.4.1) |
| ข้อมูลขาด/ผิด | ชี้ฟิลด์ที่ผิดเป็นภาษาไทย ไม่บันทึก |
| รอตรวจ / รออนุมัติ | ชิปสถานะ + คนที่ต้องทำต่อ |
| ถูกตีกลับ | เหตุผลแสดงเต็ม ไม่ตัดข้อความ |

---

## 8. ลำดับสร้าง frontend — เรียงตามเดโม ไม่ใช่ตามโครงสร้างโค้ด

| # | สร้าง | ได้อะไร |
|:---:|---|---|
| F1 | Shell + RoleSwitch + เมนูตามบทบาท + MockupBanner | สลับบทบาทให้ดูได้ |
| F2 | `/items/:id` โครงสามคอลัมน์ + KpiPair + TierBadge | **หน้าที่เดโมอยู่** |
| F3 | `/` DecisionList + KpiPair + ItemTable | เปิดมาเจอสิ่งที่ต้องตัดสินก่อนตัวเลข |
| F4 | AgentRunDrawer | พิสูจน์ว่าเป็น agent จริง |
| F5 | `/review` แท็บ A + ปุ่มยืนยันชั้น | ตัวเลข Verified ขยับต่อหน้า |
| F6 | `/my/form` ฟอร์มกรอกมือ + สถานะถูกตีกลับ | **ทางสำรองที่ต้องมีก่อนทำแชต** — ถ้าแชตไม่ทัน ยังเดโมได้ |
| F7 | `/review` แท็บ B + ResponseEditor | กฎตัวอ้างอิงมีผลจริง |
| F8 | `/documents` + RemarkRegister + export | ปิดเดโมด้วยไฟล์ที่ส่งได้ |
| **F9′** ⭐ | **`/my` ChatShell + TodoCard + ConfirmProgressCard + EvidenceResultCard + DateAskCard** | **ลูปป้อนกลับถึงคนที่แก้ได้** (AC-17 ถึง AC-20) |
| F10 | `/settings` | ผ่าน AC-11 |
| F11 | `/items` · `/audit` · `/ask` | ถ้าเหลือเวลา |

> **F1–F6 คือของที่ต้องมีจริง ๆ** · **F9′ สร้างหลัง F6 เสมอ** เพราะฟอร์มคือทางถอย
> ถ้า F9′ ไม่ทันเที่ยง 16 ก.ย. ให้เดโมด้วยฟอร์ม **ห้ามเอาแชตที่ทำงานครึ่ง ๆ ขึ้นเวที**

---

## 9. คำถามที่ยังต้องตอบ

1. **ชุดสี/โลโก้ขององค์กร** — `PLAN.md` §4.4 ระบุว่ายังไม่มี `[คำถามค้าง]` · เอกสารนี้ใช้จานสีที่ผ่าน validator ไปก่อน ถ้าได้ของจริงมาให้เปลี่ยนแค่ token ใน §3
2. **โหมดมืด** — token เตรียมไว้แล้วทั้งสองโหมด · **ข้อเสนอ: ไม่ทำในรอบนี้** เพราะต้องตรวจ contrast
   และ CVD ซ้ำอีกชุดโดยไม่เพิ่มอะไรให้เดโม — เก็บ token ไว้ เปิดทีหลังได้โดยไม่แก้ component
3. **ภาษาของ export** — §4.4 บอกว่าเอกสารส่งออกเป็นภาษาอังกฤษตามฟอร์มต้นแบบ แต่ UI เป็นไทย → หน้า `/documents` ต้องแสดงตัวอย่างภาษาอังกฤษหรือไทย
