# HANDOFF — สรุปสถานะเพื่อทำงานต่อในแชตใหม่

อ่านไฟล์นี้ก่อนเริ่มงานต่อ · ปรับปรุงล่าสุด 15 ก.ย. 2569 (commit `7d26bda`)

---

## 1. โครงการนี้คืออะไร

**GreenATM Evidence & Readiness** — ระบบติดตามความพร้อมด้านสิ่งแวดล้อมของ AEROTHAI
สำหรับ **Agentic AI Bootcamp 14–16 ก.ย. 2569 · feature freeze เที่ยง 16 ก.ย.**

| | |
|---|---|
| โครงข้อมูล | **แบบประเมินภายใน วว.นบ209_2569-14** — 4 หมวด · 24 รายการ · Level 0–5 ต่อรายการ |
| หน้าตา | ยึดตาม **`GreenATM Evidence Dashboard (standalone).html`** ที่ทีมตกลงกัน |
| แผนหลัก | **`PLAN.en.md` (rev 2.4) — ไฟล์นี้เป็นฉบับจริงเพียงไฟล์เดียว** |
| ⚠️ | **`PLAN.md` (ไทย) และ `FRONTEND.md` ยังเป็น rev 2.2 และถูก supersede แล้ว** (ดู B3 ในแผน) ทีมตัดสินใจอัปเดตเฉพาะไฟล์อังกฤษ |
| repo | https://github.com/teerapatwa/greenatm-readiness · **PRIVATE** |

---

## 2. สถานะตอนนี้ — อะไรเสร็จ อะไรไม่เสร็จ

### ✅ ใช้งานได้จริง ทดสอบแล้ว

```
verify:seed   35/35     กฎบนชุดข้อมูล (แจ้งเตือน · Suggestion · คาดการณ์ · AC-01/02/03/04/26)
verify:app   165/165    ยิง HTTP จริงในนามผู้ใช้แต่ละคน — สิทธิ์และเส้นทางการเขียน
typecheck     สะอาด
build         สะอาด
```

รันสลับลำดับ `seed → app → seed → app → app → seed` ผ่านหมดทุกรอบ
และข้อมูลกลับเป็นค่าตั้งต้นครบทุกครั้ง (เกณฑ์ 15 · สถานะ 20/2/2 · หลักฐาน 14)

**7 หน้าจอ** — หน้าแรก · งานของฉัน · รายละเอียดรายการ · แจ้งเตือน/Outbox ·
ศูนย์ตรวจสอบ · แนวโน้มและคาดการณ์ · **หลักการทำงาน** (เข้าจากท้าย sidebar ไม่นับในเมนู 1–6)

**ฐานข้อมูล** SQLite ผ่าน `node:sqlite` ที่มาในตัว Node 22 — ไม่ต้อง compile native module

**สิทธิ์ 3 บทบาท 5 ผู้ใช้** บังคับที่ route handler ไม่ใช่ซ่อนปุ่ม

| ผู้ใช้ | บทบาท | แก้ได้ | แจ้งเตือน |
|---|---|:---:|:---:|
| หัวหน้าส่วนนโยบายสิ่งแวดล้อม | เจ้าของข้อมูล | 4 รายการ (หมวด 1) | 2 |
| หัวหน้าส่วนบริหารจัดการห้วงอากาศ | เจ้าของข้อมูล | 12 (หมวด 2) | 14 |
| หัวหน้าส่วนพลังงานและสาธารณูปโภค | เจ้าของข้อมูล | 4 (หมวด 3) | 6 |
| ผู้ประสานงานระบบ GreenATM | ผู้ดูแล (ทีมกลาง) | ทุกรายการ | 8 |
| ผู้บริหารที่รับผิดชอบ GreenATM | ผู้บริหาร | ไม่ได้เลย (ยกเว้นตั้งเป้า) | **0** |

**หมวด 4 ตั้งใจไม่มีผู้ใช้เจ้าของข้อมูล** — 4 รายการนั้นผู้ดูแลเห็นคนเดียว และแจ้งเตือน 5 ฉบับ
ไม่ถึงใครเลย · ใส่ผู้ใช้สมมติจะกลบความจริงว่ารายการนั้นไม่มีใครมาอัปเดต · บันทึกเป็น **Q8**

### ⏸ ยังไม่ได้ทำ — เขียนไว้บนหน้าจอ `/how-it-works` แล้ว

| งาน | สถานะจริง | ผลที่ต้องยอมรับ |
|---|---|---|
| **M3 · agent ยิงจริง** ⭐ | `agentLoop.ts` 279 บรรทัด เขียนครบ **แต่ไม่เคยรันกับ endpoint จริงแม้ครั้งเดียว** — DGX `10.0.63.215` เป็น IP ภายใน ยิงไม่ถึงจากเครื่องพัฒนา | **ตาราง `agent_run` ว่างเปล่า · AC-05 · AC-06 · AC-07 ไม่มีหลักฐาน** · **นี่คือ B5 และเป็นชิ้นที่ทำให้โครงการเป็น agentic** |
| **M5 · เอกสารรอบเดือน + ทะเบียนหมายเหตุ** | ไม่ได้ทำ · ตาราง `response` `response_claim` `review_log` ไม่มีใน schema | **จุดขายหลักคือ "สิ่งที่ไม่มีหลักฐานต้องไม่หายจากรายงาน" แต่ยังไม่มีรายงาน** · AC-10 · AC-15 ไม่ผ่าน |
| **M4 · ลูปตีกลับพร้อมเหตุผล** | ไม่ได้ทำ | ส่วน "รายการที่ถูกตีกลับ" ของไฟล์ทีม **จงใจไม่แสดง** เพราะไม่มีข้อมูลจริง · AC-14 ไม่ผ่าน |
| **M6′ · แชตผู้ช่วยรายกอง** | ไม่ได้ทำ | AC-19 ไม่ผ่าน · ฟอร์มในหน้ารายละเอียดใช้แทนได้ |
| `agent_max_tool_calls` · `agent_timeout_seconds` · `monthly_due_day` | ยังไม่มีใครอ่าน | **ติดป้าย "ยังไม่มีผล" แบบอ่านอย่างเดียวในหน้าแจ้งเตือน** — ห้ามทำเป็นช่องกรอกที่แก้แล้วไม่เกิดอะไร |

**แต่ AC-12 ผ่าน** — ปิด endpoint แล้วทุกหน้ายังเปิดได้ กรอกได้ คำนวณได้ ไม่มีหน้าค้าง

---

## 3. กฎที่ห้ามละเมิด (สำคัญที่สุดในไฟล์นี้)

### ความปลอดภัยและความซื่อสัตย์

1. **API key อยู่ฝั่ง backend เท่านั้น** — ห้ามอยู่ในหน้าเว็บ log เอกสาร แชต หรือสกรีนช็อต
   commit ได้แค่ `.env.example` ที่เป็น placeholder
2. **ห้ามส่ง key หรือข้อมูลที่ไม่เปิดเผยข้าม `http://`** — โค้ดปฏิเสธให้แล้วใน `src/lib/llm/config.ts`
   ทดสอบแล้วว่าประตูนี้ทำงานจริง (ตอบ 503 พร้อมเหตุผล)
3. **ห้ามเปิด port ของ endpoint ออกนอก หรือลดการป้องกันเครือข่ายเพื่อให้เดโมทำงาน**
4. **ห้ามใช้ CDN ภายนอก** — ต้องเปิดได้บนเครือข่ายปิด (ฟอนต์ Noto Sans Thai จึงประกาศชื่อไว้
   แต่ไม่โหลด · เครื่องที่ไม่มีฟอนต์จะถอยไปฟอนต์ระบบ และหน้าตาจะต่างจากไฟล์ทีมเล็กน้อย)
5. **ห้ามอ้างว่าต่อโมเดลสำเร็จก่อนทดสอบ** · ถ้าใช้ mock ต้องประกาศบนหน้าจอ
6. **ห้ามส่งอีเมล/LINE/Teams จริงจากต้นแบบ** — ร่างเท่านั้น คนกดส่ง ·
   ในโปรเจกต์ไม่มี credential ของช่องทางใดอยู่เลย ซึ่งเป็นสิ่งที่ทำให้ "ส่งพลาดไม่ได้" ตรวจสอบได้
7. **`expected_tier` · `expected_cases` · `_defect` ใน `sample-data/manifest.json`
   ห้ามส่งเข้า context ของโมเดลเด็ดขาด** — เป็นเฉลยของ `verify.py`
8. **ห้ามเรียกคอลัมน์ Suggestion ว่า AI** — วันนี้เป็นกฎในโค้ด ไม่ใช่โมเดล (PLAN §5.1)
9. **ข้อมูลทั้งหมด SYNTHETIC** ติดป้ายไว้ในแอป · ผู้รับผิดชอบเป็น**ชื่อตำแหน่ง ไม่ใช่ชื่อคนจริง**

### กฎของระบบที่บังคับในระดับฐานข้อมูล

| กฎ | บังคับที่ไหน |
|---|---|
| **`audit_log.actor` ห้ามเป็น `ai`** | `CHECK (actor <> 'ai')` ใน schema + guard ใน `queries.ts` |
| ชั้นหลักฐานที่ยืนยันแล้วต้องมีคนยืนยัน | `CHECK ((confirmed_tier IS NULL) = (confirmed_by IS NULL))` |
| เป้าต้องไม่ต่ำกว่าระดับที่ได้แล้ว | `CHECK (target_level >= achieved_level)` |
| ระดับ 0 = "ยังไม่ถึงระดับ 1" เป็นค่าที่ถูกต้อง | `CHECK (achieved_level BETWEEN 0 AND 5)` — ข้อ 1.2 ใช้สถานะนี้จริง |
| **ค่าความคืบหน้าจริงเปลี่ยนที่จุดเดียว** | `confirmPending()` เรียกได้จาก `POST /api/pending/[id]` เท่านั้น |
| **ระดับขึ้นได้เฉพาะเมื่อมีหลักฐานชั้น A/B ที่ยืนยันแล้ว** | `setAchievedLevel()` — "ระดับขยับด้วยหลักฐาน ไม่ใช่ด้วยการกรอก" |
| **สองตัวเลขที่ห้ามรวมกัน** | `progress` (เจ้าของกรอก) กับ `verified` (คนยืนยันหลักฐาน) คำนวณแยกกัน — ตัวซ้ายดันตัวขวาไม่ได้ (AC-01) |

### วินัยของชุดทดสอบ

- **เทสต์ต้องคืนสภาพข้อมูลเองทุกครั้ง** — เคยพลาดแล้วสองรอบ (ดู §5)
- **ห้ามแก้ตัวเลขในเทสต์ให้ผ่าน** ถ้าเลขเปลี่ยนเพราะเผลอแตะ logic — ย้อนดูโค้ด
- **`verify:app` ยิง HTTP จริง ไม่ import ฟังก์ชันมาเรียกตรง** — เพราะ import จะข้ามการตรวจสิทธิ์
  ของ route ไปทั้งหมด แล้วเทสต์ก็ไม่ได้ทดสอบสิ่งที่อ้างว่าทดสอบ
- **ห้ามใส่ข้อมูลตัวอย่างค้างบนหน้าจอ**เพื่อให้ layout ดูครบ — ไม่มีข้อมูลก็ไม่แสดงส่วนนั้น

---

## 4. วิธีรันและทดสอบ

```bash
cd greenatm-app
npm install                  # ต้องมี Node 22.5+ (node:sqlite มาในตัว) — Node 20 รันไม่ได้
npm run build
npx next start -p 3000       # เปิด http://localhost:3000

# อีกหน้าต่างหนึ่ง
APP_URL=http://localhost:3000 npm run verify:seed   # 35/35
APP_URL=http://localhost:3000 npm run verify:app    # 165/165
```

**ล้างข้อมูลกลับเป็นค่าตั้งต้น** — ต้องปิดเซิร์ฟเวอร์ก่อน ไม่งั้นไฟล์ถูกล็อก
```bash
npm run db:reset
```

**สร้าง seed ใหม่** (หลังแก้ `scripts/gen-seed.py`) — ต้อง `db:reset` ตามด้วย
```bash
npm run gen:seed
```

**ให้เพื่อนในทีมเข้าดู** — เปิด `http://192.168.1.40:3000` (IP Wi-Fi ของเครื่องที่รัน)
Windows Firewall อนุญาต Node inbound อยู่แล้ว ไม่ต้องเปิด port เพิ่ม
⚠️ **ระบบไม่มีการยืนยันตัวตน** — ใครเปิด URL ได้ก็เลือกเป็นใครก็ได้ · Wi-Fi นี้โปรไฟล์เป็น `Public`
ถ้าเป็นเน็ตรวมที่ไม่ได้คุม ทุกคนในวงเปิดได้ · **ห้าม tunnel ออกอินเทอร์เน็ต**

---

## 5. บั๊กที่เจอแล้วแก้แล้ว — อย่าทำซ้ำ

| # | บั๊ก | บทเรียน |
|:---:|---|---|
| 1 | `alert_lead_days` ประกาศไว้แต่**ไม่มีใครอ่าน** — แจ้งเตือนกำหนดส่งไม่ทำงานเลย | **ค่าตั้งค่าที่ไม่มีใครอ่าน แย่กว่าค่าที่ไม่มี** เพราะอ่านเหมือนทำเสร็จแล้ว |
| 2 | ทำซ้ำข้อ 1 อีก **4 ตัว** ทั้งที่เขียนเตือนตัวเองไว้ใน PLAN §6.3 แล้ว | ทุกคีย์ใน `app_setting` ต้องมีเทสต์ที่เปลี่ยนค่าแล้วสังเกตพฤติกรรม |
| 3 | schema บังคับ `achieved_level BETWEEN 1 AND 5` แต่ **ระดับ 0 เป็นสถานะจริง** | **ข้อมูลถูก schema ผิด** — อย่าเดาว่าข้อมูลเสีย |
| 4 | seed ล้มแล้วยังเก็บ DB singleton ไว้ → error ชัด 1 ข้อกลายเป็น 6 ข้อที่ชี้ผิดไฟล์ | `db()` ต้องปิดแล้ว throw ไม่ cache connection ที่พัง |
| 5 | **`verify:app` ไม่คืนค่า** → รันซ้ำ 4 รอบ ทำให้ข้อ 1.1 ไล่ระดับ 2→3→4→5 ข้อมูลเดโมเพี้ยน | เทสต์ที่แก้ข้อมูลต้องคืนค่าเดิมทุกครั้ง |
| 6 | เทสต์คืนเกณฑ์เป็น **30 แบบ hardcode** ทั้งที่ค่าตั้งต้นเป็น 15 → รอบ 2 "ผ่าน" ด้วยเหตุผลผิด | คืน**ค่าที่อ่านมาจริง** ไม่ใช่ตัวเลขที่เขียนตายไว้ |
| 7 | เทสต์ไม่ลบหลักฐานที่ตัวเองสร้าง → `verify:seed` ตก 2 ข้อเมื่อรันตามหลัง | เป็นเหตุที่ `DELETE /api/evidence/[id]` เข้ามา (และเป็นฟีเจอร์ที่ควรมีจริง) |
| 8 | **สถานะ `at_risk` เกิดขึ้นไม่ได้เลย** — ช่องว่างสูงสุด 20 แต่เกณฑ์ 30 และแผนขั้น 2 ทุกข้อเริ่มหลังวันอ้างอิง | สถานะ 3 แบบต้องโผล่ได้ครบ · ลดเกณฑ์เป็น 15 + ให้ 2 ข้อมีขั้นกำลังดำเนินอยู่ |
| 9 | **Level 1 ไม่มีสี** — เอา `#edf0ea` ที่เป็น**พื้นราง**มาใช้เป็นสีข้อมูลด้วย | แยก token `--track` ออกจาก `--l1..--l5` · **รัน `dataviz/validate_palette.js --ordinal` ไม่ใช้ตาวัด** (ramp เดิมปลายอ่อนได้ 1.30:1 ต่ำกว่าพื้น 2:1 ทั้งชุด) |
| 10 | `AC-26` — Suggestion ขึ้น "เสร็จสมบูรณ์" กับข้อที่ถึงเป้าแต่ไม่มีหลักฐาน | `rules.ts` **ตรวจหลักฐานก่อนตรวจเป้า** · แก้แล้วใน app · **`dashboard.html` ยังมีบั๊กนี้อยู่** |
| 11 | Windows `cp1252` `UnicodeEncodeError` ตอน print ภาษาไทยใน Python | ใส่ `sys.stdout.reconfigure(encoding="utf-8")` หรือ `PYTHONIOENCODING=utf-8` |
| 12 | Bash heredoc พังกับสตริงยาวที่มีอักขระพิเศษ/ไทย | **ใช้ Write tool เขียนสคริปต์ยาว** ไม่ใช้ heredoc |
| 13 | `npm run lint` ค้างรอ input (`next lint` deprecated · `eslint-config-next@15.5` เข้ากับ ESLint 9 ไม่ได้) | ลบ script ที่พังออก · ใช้ `npm run typecheck` แทน |
| 14 | Next.js 15.1.6 มี **CRITICAL RCE บน Windows-hosted server** | อัปเป็น 15.5.25 แล้ว · เหลือ moderate 1 + high 1 ใน `postcss@8.4.31` ที่ Next pin มาเอง (build-time, CSS ของเราเอง — รับได้) |

---

## 5.1 ข้อมูลที่ต้องรู้เรื่องชุดตัวอย่าง

- **19 จาก 24 ข้อไม่ขยับระดับจากปีที่แล้ว** ← **ตัวเลขที่แรงที่สุดในเดโม ควรตั้งใจใช้**
- ระดับเฉลี่ย 1.83 · ปีที่แล้ว 1.62
- ยังไม่ส่งรอบนี้ 4 รายการ (2.10 · 3.2 · 3.4 · 4.4) · กำหนดส่ง 2026-09-20 (อีก 5 วัน)
- เลื่อนแผน 3 ครั้ง: **2.10 · 3.3** → ยกระดับถึงผู้ดูแล
- ไม่มีหลักฐานเลย 13 รายการ · เอกสารไม่มีวันที่: 2.10 · 3.3 · เอกสารล้าสมัย: 2.9 · 3.2
- **2.11 · 2.12 อยู่ Level 5 ถึงเป้าแต่ไม่มีหลักฐานเลย** ← เคส AC-26
- แจ้งเตือน: ผู้ดูแล 8 · เจ้าของรวม 27 · ผู้บริหาร 0 · **ไม่มีผู้รับ 5 (หมวด 4)**
- สถานะ: on_track 20 · at_risk 2 · delayed 2

---

## 6. ทำอะไรต่อ — เรียงตามคุ้มค่า

| # | งาน | เหตุผล |
|:---:|---|---|
| **1** | **ยิง `POST /api/llm/tf6` และ AC-06 จากเครื่องที่ถึง DGX** | **ไม่ต้องเขียนโค้ดเลย ติดแค่เครือข่าย** · ปลดล็อก AC-05 · AC-06 · AC-07 · AC-19 ทีเดียว · เป็นชิ้นเดียวที่ทำให้โครงการเป็น agentic แต่ไม่มีหลักฐาน (B5) |
| **2** | **ซ้อมเดโม 3 นาที จับเวลา** | แอปรันอยู่แล้ว เดินได้เลย · เดโมที่พังบนเวทีแพ้ทันทีไม่ว่าโค้ดจะดีแค่ไหน |
| **3** | **ส่ง `PLAN.en.md` ให้ mentor** | `PLANNING.md` Stage 5 กำหนดให้รีวิวก่อนลงมือ — เราลงมือไปแล้ว **ต้องบอกตรงๆ** พร้อมแจ้ง D7–D20 ที่เปลี่ยนขอบเขต Required |
| 4 | **M5 เอกสารรอบเดือน + ทะเบียนหมายเหตุ** | จุดขายหลักของโครงการที่ยังไม่มี · ~4 ชม. |
| 5 | M4 ลูปตีกลับ (ปลดล็อก AC-14 + ส่วน "รายการที่ถูกตีกลับ" ของไฟล์ทีม) | ~2 ชม. |
| 6 | M6′ แชตผู้ช่วยรายกอง (AC-19) | ~3 ชม. · การ์ดยืนยันมีอยู่แล้ว เหลือเปลือกแชต |
| 7 | แก้ `dashboard.html` (mockup) ให้ AC-26 ตรงกับแอป | ถ้ายังจะใช้ mockup pitch · ตอนนี้สองไฟล์ตอบไม่เหมือนกัน |
| 8 | back-port `PLAN.md` (ไทย) + `FRONTEND.md` | B3 · ถ้าไม่ทำ ต้องบอก mentor ว่าอ่านไฟล์ไหน |

**ข้อ 1 ทำแทนไม่ได้** — ต้องรันจากเครื่องในเครือข่ายที่ถึง DGX `10.0.63.215`
พิมพ์ `! npm run check:llm` ในแชตได้เลยถ้าย้ายเครื่องแล้ว

---

## 7. แผนที่ไฟล์

```
PLAN.en.md                      ⭐ แผนฉบับจริง rev 2.4 — อ่านไฟล์นี้
PLAN.md · FRONTEND.md              rev 2.2 ถูก supersede แล้ว (B3)
HANDOFF.md                      ⭐ ไฟล์นี้
GreenATM Evidence Dashboard (standalone).html   ต้นแบบหน้าตาของทีม (สกัด theme มาจากไฟล์นี้)
dashboard.html · mockup.html       mockup รุ่นก่อน (dashboard.html ยังมีบั๊ก AC-26)
DGX-SPARK-INTEGRATION.md · QWEN-FPT-INTEGRATION.md   คู่มือ endpoint จาก facilitator
sample-data/                       ชุดข้อมูล CANSO 20 หัวข้อ + หลักฐาน 24 ไฟล์ (ของ mockup)
tools/                             สคริปต์สร้าง mockup + check_endpoint.py (TF1–TF8)

greenatm-app/                   ⭐ แอปจริง
  src/lib/data/rules.ts         ⭐ กฎทั้งหมด เป็น pure function — แจ้งเตือน · Suggestion ·
                                   statusFor · gapsFor · คาดการณ์ · ไม่มีการเรียกโมเดล
  src/lib/db/schema.ts             โครงฐานข้อมูล + CHECK ที่บังคับกฎ
  src/lib/db/queries.ts            อ่าน/เขียน + audit + การ์ดยืนยัน + แผนงาน + slip + ไฟล์
  src/lib/db/index.ts              DatabaseSync + seed ครั้งแรก + migrate()
  src/lib/auth/perms.ts         ⭐ บทบาทและสิทธิ์ — ความจริงชุดเดียว
  src/lib/auth/session.ts          ตัวตน (cookie) + requireAbility + requireItemWriteAccess
  src/lib/view.ts                  ประกอบข้อมูลตามผู้ใช้ — การกรองอยู่ที่นี่ที่เดียว
  src/lib/llm/                     ชั้นเชื่อมต่อโมเดล + agentLoop (ยังไม่เคยรันจริง)
  src/app/                         7 หน้า + API 20 เส้นทาง
  src/components/                  ui.tsx (presentational) · actions.tsx · MilestoneEditor ·
                                   ItemAdmin · TargetDots · ReviewTabs · EvidenceDelete · Shell
  scripts/gen-seed.py              สร้าง seed.json แบบกำหนดผลตายตัว
  scripts/verify-seed.mjs          35 ข้อ
  scripts/verify-app.mjs        ⭐ 165 ข้อ ยิง HTTP จริง
```

---

## 8. สิ่งที่ต้อง**ไม่**พูดบนเวที

1. **ห้ามบอกว่าคอลัมน์ Suggestion เป็น AI** — เป็นกฎในโค้ด
2. **ห้ามบอกว่าแจ้งเตือนส่งข้อความได้** — ร่างเท่านั้น ไม่มีช่องทางส่งในโปรเจกต์
3. **ห้ามบอกว่า agent ทำงานแล้ว** จนกว่าจะยิง TF6 ผ่านจริง
4. **ห้ามบอกว่าระบบตัดสินว่าองค์กรผ่านการประเมิน** — ไม่มีใครในระบบมีอำนาจนั้น
   ระบบบอกได้แค่ว่าหลักฐานพร้อมให้ประเมินหรือยัง
5. **การคาดการณ์ 3–5 ปีเป็นการฉายภาพ ไม่ใช่คำมั่น** — สมมติฐานอยู่บนหน้าจอเดียวกับตัวเลข
   ถ้าตัดบล็อกสมมติฐานออก ต้องตัดการคาดการณ์ออกไปด้วย
6. **ชั้นหลักฐาน A/B/C/D เป็นของเราเอง ไม่ใช่ของ CANSO** (PLAN §6.5) —
   คนที่รู้เอกสาร CANSO จะจับได้ และการอ้างว่าเป็นของเขาจะทำให้ส่วนอื่นเสียความน่าเชื่อถือ
